# H723VGT6-上层代码框架规范

## 1. 本轮范围

当前已经整理的板端工程标记为 `H723VGT6-上层`。本文件只记录上层工程的分层、三路
FDCAN、通信和 FreeRTOS 框架规范，不包含下层设计。本轮没有加入 UI、Tool、Python、
Log 或 Auto Test 调试层代码。

当前业务入口已按机械臂、传送带和夹爪整理；目标任务名称、优先级关系、周期和通信边界仍是
后续实现时必须遵守的目标设计，不表示对应的独立任务函数已经全部实现。

代码调用方向固定为：

```text
Application（Robot / Task / State Machine）
    -> App（机械臂 / 传送带 / 夹爪）
        -> Algorithm / Driver
            -> BSP / HAL
```

上层不得跨过 Driver 直接调用 HAL，算法层不得发送 CAN，业务层不得直接
操作寄存器。

## 2. H723VGT6-上层 目标目录和职责

```text
H723VGT6-上层/
|-- Core/                         CubeMX 生成入口和 HAL 外设初始化
|-- User/                         队内自写和自行维护的全部代码
|   |-- BSP/                      硬件层
|   |   |-- Can/                  三路 FDCAN 的 HAL 发送封装
|   |   `-- Communication/        UART DMA、FDCAN RX 中断转任务
|   |-- Driver/                   驱动层
|   |   |-- Can/                  与 HAL 无关的 CAN 帧类型
|   |   |-- Motor/                电机配置、周期调度、型号适配端口
|   |   |   |-- DJI/               DJI 1-4 / 5-8 组帧公共封装
|   |   |   |-- DM J4310/          达妙 J4310 MIT 协议
|   |   |   |-- LK MG5010E-i36/    LK MG5010E-i36 协议
|   |   |   |-- M2006/             M2006 + C610 协议
|   |   |   `-- M3508/             M3508 + C620 协议
|   |   `-- PcLink/               小电脑二进制帧、CRC16 和流式解析
|   |-- Algorithm/                算法层
|   |   `-- Pid/                  纯 PID 计算，不访问硬件
|   |-- App/                      业务层
|   |   |-- Arm/                  J4310 + 2 x M3508
|   |   |-- Conveyor/             1 x M2006
|   |   `-- Gripper/              1 x M2006
|   |-- Application/              应用层
|   |   |-- Communication/        小电脑命令解码、状态组包、超时判断
|   |   |-- Robot/                整机状态机和业务模块编排
|   |   `-- Task/                 FreeRTOS 任务入口、任务间通信和安全协调
|   |-- Config/                   参数和 ID 集中配置
|   |   |-- can_id.h
|   |   |-- upper_config.h
|   |   `-- upper_config.c
|   `-- Tests/                    不加入固件的主机侧回归测试
|-- Drivers/                      ST 官方 HAL/CMSIS，不是自写 Driver 层
|-- Middlewares/                  FreeRTOS
`-- MDK-ARM/                      Keil 工程
```

`User/Driver/` 和 ST 自动生成的 `Drivers/` 名字相近但职责不同：前者是队内设备驱动，
后者是 STM32 固件包，不能混放。工程根目录不得再新增与 `User/` 平级的自写分层目录。

上面的 `User/App/` 是当前生效的职责划分。旧 `Move/`、`Ore/`、`Gate/` 目录只作为未编译的
历史代码保留，不得重新加入工程或与 `Arm/`、`Conveyor/`、`Gripper/` 同时驱动同一台电机。

## 3. H723VGT6-上层 采用的代码规范

完整要求以仓库根目录 `控制组代码规范.md` 为准，H723 框架执行以下规则：

- 普通变量使用小写加下划线，如 `target_vel_rad_s`；物理量必须保留 `_rad`、`_rad_s`、
  `_a`、`_ms` 等单位后缀。
- 宏全部大写；枚举和结构体类型统一使用 `_t` 后缀；函数使用 `模块_动作()` 形式，
  如 `Arm_Update()`、`PID_Calc()`、`BspCan_Send()`。
- 每个功能使用独立 `.c/.h`，文件内部能设为 `static` 的变量和函数不暴露为全局符号。
- `.c` 文件顺序固定为 include、宏、私有类型、static 数据、全局数据、函数；不在业务
  文件里混入 HAL 初始化或其他机构的逻辑。
- `if/else` 和 `switch` 使用统一大括号格式，每个 `case` 必须明确 `break`；禁止用大量
  分散条件判断代替状态机。
- 周期、限幅、超时、ID 和机构参数必须集中到 `User/Config/`，禁止在业务函数中散落魔法数字。
- CAN ID 只在 `User/Config/can_id.h` 或具体协议驱动中定义；算法层不发 CAN，业务层不拼报文。
- 控制周期顺序固定为“读取反馈 -> 状态更新 -> PID/算法 -> 业务目标 -> CAN 调度发送”。
- ISR 只取数据和通知任务，不解析完整协议；任务中禁止使用阻塞式 UART/CAN 发送。
- 对外接口检查空指针、长度、状态和范围；通信超时后先停输出，不复用过期目标。
- CubeMX 再生成时只允许覆盖生成区；自写代码统一放在 `User/` 的对应分层中。`Core/` 仅在
  `USER CODE BEGIN/END` 区保留初始化和任务入口的最小桥接，不承载业务、驱动或算法实现。

本轮按要求不实现调试层。以后即使加入调试功能，也不得在业务代码里散落 `printf`，更不能
让机器人核心闭环依赖小电脑脚本。

## 4. H723VGT6-上层 三路 CAN、机构分配和余量

三路均为 1 Mbps 经典 CAN，H723 控制任务和目标发送周期均按 1 ms 设计。

| FDCAN | 机构 | 电机 | 逻辑节点 ID |
| --- | --- | --- | --- |
| FDCAN1 | 机械臂 | J4310 | 3 |
| FDCAN2 | 机械臂 | M3508/C620 #1、#2 | 1、2 |
| FDCAN3 | 传送带、夹爪 | Conveyor M2006、Gripper M2006 | 1、2 |

负载按 11 位标准 ID、8 bytes 经典 CAN 数据帧估算：无位填充约 111 bit/帧，工程保守值
取 135 bit/帧。余量定义为 `100% - 持续总线占用率`，目标至少保留 20%，用于吸收位填充、
仲裁等待、错误重发、使能/诊断报文和任务抖动。

| FDCAN | 1 ms 内保守帧数 | 估算占用 | 估算余量 | 当前结论 |
| --- | ---: | ---: | ---: | --- |
| FDCAN1 | J4310 控制/反馈，共 2 帧 | 27.0% | 73.0% | 可按 1 kHz 实施 |
| FDCAN2 | 2 反馈 + 1 个 DJI 组控制帧，共 3 帧 | 40.5% | 59.5% | ID 1、2 同组时可实施 |
| FDCAN3 | 2 反馈 + 1 个 DJI 组控制帧，共 3 帧 | 40.5% | 59.5% | ID 1、2 同组时可实施 |

逻辑节点 ID 集中在 `User/Config/can_id.h`。具体仲裁 ID 由各型号驱动换算，业务层不出现
`0x200`、`0x201` 一类协议数字。FDCAN2 的两台 M3508 设置为 ID 1、2，共用一个 `0x200`
组控制帧；M3508 的目标、反馈、PID 和超时状态按“CAN 总线 + 节点 ID”寻址。每个组帧的
第三、第四槽位始终发送零电流。

J4310 使用电机 ID 3、Master ID 0，两项必须与达妙调试助手中的持久配置一致。首次启动时
一个周期只发送使能帧，下一个周期才开始发送 MIT 控制；使能后允许 50 ms 建立首个反馈，
状态帧不会与常规 MIT 帧在同一周期叠加。FDCAN1 只放行 Master ID 0，FDCAN2 只放行
`0x201`、`0x202`。

FDCAN3 的两台 M2006/C610 共用 `0x200` 组控制帧。节点 1 使用第一个电流槽位，节点 2
使用第二个电流槽位，未配置设备对应的第三、第四槽位始终发送零电流。
FDCAN3 的硬件接收过滤器只放行对应反馈 ID `0x201` 和 `0x202`，不再接收节点 3、4
或其他未配置设备的报文。

## 5. H723VGT6-上层 小电脑虚拟串口

依据 `培训H723VGT6.tel`，Type-C 的 D+/D- 连接 CH340N，不是直接连接 STM32H723。
因此通信路径为：

```text
小电脑 USB
    -> CH340N 虚拟串口
        -> H723 UART4（PA0 TX / PA1 RX）
            -> RX 循环 DMA / TX 普通 DMA
```

小电脑在 Windows 下看到 `COMx`，Linux 下通常看到 `/dev/ttyUSBx`。板端继续使用
UART4 115200 8N1；不额外启用 H723 USB CDC。115200 适合控制命令和 20 Hz 状态回传，
不用于持续输出高频原始波形。

串口使用二进制帧，不能直接发送裸结构体或无校验文本：

```text
A5 5A | version | type | sequence(u16) | length(u16) | payload | CRC16
```

- 最大载荷 128 bytes，所有多字节字段为小端。
- `PC_MSG_HEARTBEAT`：心跳。
- `PC_MSG_ESTOP`：显式急停。
- `PC_MSG_UPPER_CMD`：上层机构目标，共 34 bytes；2-byte 使能掩码后依次为 J4310
  位置/速度/Kp/Kd、M3508 #1-#2 速度、传送带 M2006 速度和夹爪 M2006 速度，共 8 个
  小端 `float`。本拓扑使用协议 V3，旧 V1/V2 和 42/50/58 bytes 命令均不再接受。
- `PC_MSG_ROBOT_STATE`：H723 以 20 Hz 回传状态和发送统计。
- CRC16 覆盖 `version` 到 `payload`，错误帧直接丢弃并重新同步。

通信任务只负责解析并把目标写入 RTOS 队列；对应的 1 ms 业务任务读取队列后才修改机构目标。
进入远程控制后，连续 200 ms 没有有效帧会执行普通停机；显式急停进入 `ROBOT_ERROR`，
不能靠下一帧普通目标自动解除。没有连接小电脑时，H723 的本地状态机仍可独立运行。

## 6. H723VGT6-上层 FreeRTOS 任务框架

上层不再以一个 `appTask` 承担全部机构控制。业务任务按机构拆分，具体任务函数本轮保持
空缺，只规定以下边界：

| 任务 | 负责机构 | 触发方式 | 职责边界 |
| --- | --- | --- | --- |
| `upperSafetyTask` | 上层全部输出 | 事件唤醒，并以 1 ms 超时周期复查 | 最高业务优先级；汇总急停、通信超时、电机离线和任务超期，发布全局输出禁止状态 |
| `upperArmTask` | J4310 + 2 x M3508 | 固定 1 ms 周期 | 机械臂状态机、目标约束和控制计算 |
| `upperConveyorTask` | 1 x M2006 | 固定 1 ms 周期 | 传送带状态机、目标约束和控制计算 |
| `upperGripperTask` | 1 x M2006 | 固定 1 ms 周期 | 夹爪状态机、限位判断和控制计算 |

`commRxTask`、`motorTxTask` 和 `monitorTask` 属于基础设施任务，不算机构业务任务：

- `commRxTask` 由 FDCAN/UART 中断通知后运行，只负责清 RX FIFO、协议解码、反馈快照和
  命令队列，不执行机构状态机。
- `motorTxTask` 是周期发送的唯一所有者。各业务任务只提交带周期号的目标快照并设置对应的
  周期完成位；发送任务等待全部完成位或本周期截止时刻，在安全校验后统一经 `User/Driver/`
  调用 `User/BSP/` 发送。某个业务任务缺席或目标过期时，只能对对应机构使用安全目标并记录
  超期，不能复用旧目标。禁止三个业务任务并发操作同一 FDCAN HAL 句柄或自行拼 CAN 报文。
- `monitorTask` 为最低优先级，每秒记录任务栈、堆余量、周期超期、队列溢出和 CAN 错误，
  不在控制路径输出日志。

相对优先级固定为 `upperSafetyTask > commRxTask > motorTxTask > 机构业务任务 >
monitorTask`。三个机构业务任务使用同一优先级，不能靠提高某个机构任务的优先级掩盖超期；
CMSIS-RTOS2 的具体优先级枚举和各任务栈大小要在实现时根据最坏执行时间与栈水位确定。

整机状态为 `ROBOT_INIT -> ROBOT_READY -> ROBOT_RUN`，另有 `ROBOT_STOP` 和
`ROBOT_ERROR`。安全任务发布的禁止状态优先于任何业务目标；解除急停必须经过明确的状态机
条件，不能由下一帧普通目标自动清除。安全停止路径不得等待业务任务持有的互斥量。

每个 1 ms 控制周期的逻辑顺序固定为“读取已发布的反馈快照 -> 更新安全和整机状态 ->
各机构计算目标 -> 校验目标周期号和有效期 -> 电机周期调度 -> CAN 发送”。业务任务之间
通过队列、线程标志或双缓冲快照传递数据，不直接读写另一任务的可变私有数据。

## 7. H723VGT6-上层 FreeRTOS 内核规范

本节落实 `FreeRTOS.md` 中记录的内核、中断和内存问题。

### 7.1 内核版本、异常和 Tick 归属

- 使用已经验证的 FreeRTOS V10.6.2、CMSIS-RTOS2、Cortex-M7 端口和 `heap_4`
  组合作为基线。不得混用其他版本的头文件、`cmsis_os2.c`、portable 端口或 heap
  文件；工程中只能链接一套内核源码和一个 heap 实现。升级版本必须重新编译并复测任务切换、
  队列、中断通知、栈溢出和 malloc 失败路径。
- 使用抢占式调度，`configTICK_RATE_HZ` 为 1000，即 1 Tick 为 1 ms。FreeRTOS 使用
  SysTick，HAL 时基使用 TIM6；不得让 HAL 和内核同时接管 SysTick。
- `SVC_Handler`、`PendSV_Handler` 和 `SysTick_Handler` 只能由 Cortex-M7 FreeRTOS 端口
  接管一次。CubeMX 重新生成后，必须检查向量表和中断文件中没有重复
  定义；PendSV 和 SysTick 保持最低可抢占优先级。
- Tick 到期只代表任务变为 Ready，不保证此刻已经运行。1 ms 周期任务必须使用绝对周期
  `osDelayUntil()`，保存并递增自己的 `next_tick`，同时统计迟到和执行超期；不能使用
  `osDelay(1)` 冒充固定 1 ms 周期，更不能用不阻塞的轮询循环维持周期。

### 7.2 中断优先级和 ISR 边界

- 使用 `configLIBRARY_MAX_SYSCALL_INTERRUPT_PRIORITY = 5`。Cortex-M 的 NVIC
  数字越小，实际优先级越高：数值 0-4 的中断禁止调用任何 FreeRTOS/CMSIS-RTOS2 接口；
  需要通知任务的 FDCAN、UART、DMA 等中断必须设置为数值 5-15。配置时使用库优先级宏，
  不手工重复进行位移换算。
- ISR 只清硬件标志、搬取必要的固定长度数据并通知任务，不能在 ISR 中循环解析完整协议、
  做控制计算、格式化输出、动态分配内存、等待互斥量或调用阻塞接口。原生 FreeRTOS API
  必须使用 `FromISR` 版本，队列等待时间必须为 0，并按 API 返回值请求中断退出后的调度。
- 高于内核系统调用边界的中断只能处理完全独立的硬实时数据，不能间接调用会进入 RTOS 的
  HAL 回调。所有新 IRQ 在启用前必须明确设置优先级，不能依赖复位默认值 0。

### 7.3 任务通信、共享资源和安全状态

- CAN 帧、机构命令和反馈快照使用定长队列或双缓冲按值传递；线程标志/任务通知只表达“有
  事件”或周期完成位，不能用一个 bit 统计多次事件。安全事件使用锁存状态，队列满或通知
  合并都不能静默丢失急停。
- 共享 UART/SPI 等外设采用“单一任务拥有 + 其他任务发消息”的方式；确实存在多任务直接
  访问时才使用互斥量。互斥量不能在 ISR 使用，持锁期间禁止延时、打印或等待另一把锁，所有
  退出路径都必须释放，并统一多锁获取顺序。
- `volatile` 不能替代队列、临界区、原子操作或 Cache 一致性处理。业务任务只消费已发布的
  完整快照；更新多字段状态时不能让另一个任务读到半新半旧的数据。
- 安全状态是所有电机目标的最终门控条件，优先级高于业务命令。看门狗只能在安全任务确认
  必要任务均更新心跳、无关键超期后喂狗，不能由某一个始终运行的任务单独喂狗。

### 7.4 栈、堆和故障钩子

- 任务栈、FreeRTOS 堆、C 库堆和中断使用的 MSP 是四类独立内存，必须分别核算。CMSIS-
  RTOS2 的 `stack_size` 单位是 bytes，原生 `xTaskCreate()` 的栈深单位是
  `StackType_t` 数量，在 Cortex-M7 上换算时不得直接照抄数值。
- `heap_4` 和 64 KiB `configTOTAL_HEAP_SIZE` 作为初始基线，不作为容量已经足够的证明。
  任务、队列和互斥量集中在启动阶段创建，每次检查返回句柄；运行阶段禁止反复创建对象。
- 必须启用 `configCHECK_FOR_STACK_OVERFLOW = 2`、malloc 失败钩子和 `configASSERT`。监控任务
  至少记录每个任务的历史最小栈余量、当前与历史最小 RTOS 堆余量、MSP 余量、malloc 失败、
  栈溢出任务名和 HardFault 寄存器。栈大小只能根据覆盖最长调用链的实测水位调整。

### 7.5 STM32H7 D-Cache 与 DMA

- H723 开启 D-Cache 后，所有 UART/SPI/ADC 等 DMA 缓冲区必须先确认所在 RAM 可被对应 DMA
  访问，再处理 Cache 一致性；不能因为代码能编译就认为 DMA 一定能访问该地址。
- D-Cache line 为 32 bytes。DMA 缓冲区起始地址和占用范围按 32 bytes 对齐，并尽量独占
  Cache line。DMA 发送前 Clean；DMA 接收启动前按统一封装执行 Clean/Invalidate，完成后
  CPU 读取前再次 Invalidate。DMA 完成前 CPU 不得改写仍归 DMA 所有的缓冲区。
- 也可以由 MPU 建立专用 non-cacheable DMA 区域，但工程只能选定一种统一封装
  策略，不能让每个驱动自行维护 Cache。最终地址必须通过链接脚本和 `.map` 文件核对。

## 8. H723VGT6-上层 当前接口边界

`User/Driver/Motor/` 保留 MG5010 驱动源码，当前正式拓扑接入 J4310、M3508/C620 和 M2006/C610，统一编码为
`can_frame_t` 后调用 `BspCan_Send()`。`UpperMotorPort` 负责通用目标到型号命令的转换、
首次使能、反馈分发和 DJI 组帧，业务层不拼协议字节。型号驱动已按目录归档，公共的 DJI
`0x200/0x1FF` 组帧放在 `User/Driver/Motor/DJI/`，避免 M3508 与 M2006 在同一总线上重复发送组帧。

- J4310 当前按 MIT 模式发送。电机调试助手中保存的控制模式、`PMAX/VMAX/TMAX`、电机
  CAN ID 和 Master ID 必须分别与 `upper_config.h`、`can_id.h` 一致；代码不能通过普通
  运行帧替代持久模式配置。使能帧与 MIT 控制帧分周期发送，首次反馈宽限为 50 ms。
- M3508 的速度和位置闭环在 H723 上完成。FDCAN2 的 ID 1、2 共用一个 `0x200` 控制帧；
  端口先缓存每台电机目标，在每个 1 ms 周期末发送组帧。驱动上下文按总线和节点二维隔离；
  没有有效反馈、反馈超过 50 ms 或命令超时后，对应槽位强制为零电流并复位对应 PID。
- 初始 M3508 电流限制为 3 A，PID 仅是移植基线，必须在卸载或架空、具备急停条件时重新
  实机整定。电机方向、机械零位和限位也必须按机构验收。
- M2006/C610 已接入电流、速度和位置三种目标模式，减速箱输出轴单位为 `rad`、`rad/s`。
  当前两台 M2006 均在 FDCAN3，分别使用 ID 1 和 2。M2006 初始电流限制为 2 A，PID 仅按
  外部参考驱动折算为 H723 基线，必须实机重新整定。
- FDCAN3 的传送带和夹爪 M2006 分别保存反馈与闭环状态；任一反馈超过 50 ms 或命令
  超时，只清零对应槽位并复位对应 PID。编码器、IMU 等设备也按相同分层加入 `User/Driver/`，
  不能塞进业务模块。

## 9. H723VGT6-上层物理层和验收要求

- 每条 CAN 使用线型主干或菊花链，支线尽量短；每条独立物理总线只在两端各放一个
  120 欧终端，断电测 CAN_H 与 CAN_L 应约为 60 欧。
- 每路 CAN 都必须有独立收发器，主控、电机和电调必须共参考地。
- 每路 RX FIFO 至少保留 8 个元素；过滤器按总线和标准/扩展帧明确规划，
  不能长期全部放行后靠业务层猜协议。
- 联调时记录实际 bus load、最大报文延迟、发送队列峰值、RX FIFO 丢帧、TEC/REC、
  Bus-Off 和控制周期超时。
- FDCAN1 必须在一台 J4310 运行的 1 kHz 最坏工况下记录 bus load、最大报文延迟、丢帧
  和 Bus-Off。80% 仅为负载评估参考，不作为初始化条件。
- FDCAN2 必须在两台 M3508 同时运行的 1 kHz 最坏工况下记录 bus load、最大报文延迟、
  丢帧和 Bus-Off；未完成实测前只能称为“可按 1 kHz 实施”。
- FDCAN3 必须在传送带与夹爪两台 M2006 同时运行的 1 kHz 最坏工况下记录 bus load、
  最大报文延迟、丢帧和 Bus-Off；未完成实测前只能称为“可按 1 kHz 实施”。

## 10. 为什么这样选择

1. H723VGT6-上层有三路片内 FDCAN，可以按机构和协议拆分物理总线，比在一条总线上继续
   叠加报文更容易控制负载和定位故障。
2. 业务层只提交物理目标，电机协议留在 Driver，后续更换电机或 CAN ID 时不会改机械臂、
   传送带或夹爪状态机。
3. 各机构 1 ms 业务任务满足已经确定的 1 kHz 目标；中断只通知通信任务，发送由一个任务
   统一调度，避免在 ISR 内解析协议或让多个任务争用 HAL 句柄，控制周期更可预测。
4. 小电脑采用现有 CH340N 虚拟串口，不需要改 PCB，也避免误启用没有接到 Type-C 的
   H723 USB CDC；DMA 和带 CRC 的定长协议比文本命令更适合比赛现场连续通信。
5. 小电脑只下发目标和读取状态，急停、超时和整机状态机留在板端，因此拔掉 USB 后核心
   控制逻辑仍然成立。
6. MG5010 从正式拓扑移除后同步清理配置、业务目标和通信载荷，但保留独立驱动源码供后续
   复用；三路 FDCAN 继续服务当前七台电机。
