# H723VGT6 上层 User 目录整理对话上下文

记录时间：2026-09-03

## 用户要求

### 第一条

根据 `D:\桌面\暑期培训\暑期培训-B组\上层分支\H723VGT6-上层\User` 完善
`D:\桌面\暑期培训\暑期培训-B组\上层分支\H723VGT6-上层-修改\User`。

- `Algorithm/Gravity compensation` 只放电机重力补偿算法的公共部分。
- `Algorithm/PID` 只放 PID 算法的公共部分。
- `APP` 和 `Application` 按之前的方式整理。
- `BSP/CAN` 放所有 CAN 底层内容。
- `BSP/UART` 放所有 UART 底层内容。
- `BSP/Flash` 放所有 Flash 底层内容。
- `Communication/Chassis` 放上层 MCU 与底盘 MCU 的发送、接收和通信内容。
- `Communication/Computer` 放上层 MCU 与小电脑的发送、接收和通信内容。
- `Communication/Small board` 放上层 MCU 与 F103 小板的发送、接收和通信内容。
- `Motor` 放电机通信收发、电机模式和电机控制内容。
- 遥控器先把数据发给 F103 小板，再由底盘 MCU 原样转发给上层 MCU，因此遥控通信和解析放在 `Communication/Small board`。
- `Application/Task` 只放接收到按键之后的动作流程，不放 UART、CAN、电机协议等底层内容。

### 第二条

- 内容要简洁，不增加没用的保护、诊断等代码。
- 电机控制保持“读取电机反馈、计算控制量、发送指令”的直接流程。
- 每个 `.c`、`.h` 文件都要有相应中文注释，风格参考原工程 `User` 目录。

### 最新补充

- 将当前对话保存到本文件。
- **角度转换不要动，后续不得移动或修改角度转换相关代码。**

## 当前整理结果

- 公共 PID 和重力补偿已分别放入 `Algorithm/PID`、`Algorithm/Gravity compensation`。
- CAN、UART、Flash 的 HAL 访问已分别集中在 `BSP/CAN`、`BSP/UART`、`BSP/Flash`。
- 底盘、小电脑、F103 小板通信已分别放入对应的 `Communication` 子目录。
- F103 小板目录包含遥控数据解析以及小板输出控制帧发送。
- 电机反馈、模式、控制计算和指令发送已放入 `Motor`，公共电机入口位于 `Motor/All-Common`。
- M2006、M3508、J4310 的内容分别放在各自电机目录。
- `Application/Task/task.c` 只保留遥控按键上升沿和动作目标流程。
- 按原工程保留 PD13、PD12、PD11、PD8、PD9、PD10 以及 PC0、PC1 的目标动作。
- 主程序已调用 `My_Init()`，FreeRTOS 控制任务和通信初始化任务已接入。
- Keil 工程文件已加入 User 源文件和头文件搜索路径。
- `User/README.md` 已记录目录职责。

## 验证状态

- 用户代码 C 文件语法检查通过：0 个错误、0 个警告。
- Keil 工程 XML 解析通过。
- 工程文件引用和头文件目录引用均存在。
- 全部 `.c`、`.h` 文件均带中文文件注释。
- `Application/Task` 扫描结果中没有 HAL、FDCAN、UART 句柄或 BSP 依赖。
- 当前环境未找到 Keil 命令行程序，因此没有执行完整 Keil 链接构建。

## 后续约束

1. 不移动、不修改角度转换相关代码。
2. `Application/Task` 只修改按键后的动作流程。
3. 遥控收发和解析继续放在 `Communication/Small board`。
4. CAN、UART、Flash 的 HAL 调用不得移出各自 BSP 目录。
5. 新增文件继续使用简洁的中文注释。
