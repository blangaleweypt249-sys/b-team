# J4310 排查对话上下文

更新时间：2026-08-14

## 用户需求

1. 确认当前代码是否对 J4310 发送了使能命令，未使能时无法进行 MIT 控制。
2. 修改为：J4310 使能后持续保持使能，只有在电机真正报错时才自动失能。
3. 解决录屏中的问题：电机未使能、界面反复变化/重握手、反馈角度一直为 `--`，以及勾选“允许 J4310 输出”后无效。
4. 用户已确认 CAN 物理链路正常，J4310 电机 ID 为 `0x06`，Master ID 为 `0x16`，要求继续修改代码并找出未使能原因。

## 已确认的协议与现象

- J4310 使用 FDCAN1，标准帧 ID 应为 `0x006`。
- MIT 使能帧应为 8 字节：`FF FF FF FF FF FF FF FC`。
- J4310 反馈帧 ID 为 `0x016`，`data[0]` 低 4 位为电机 ID，高 4 位为状态码。
- 录屏中最近反馈为 `CAN ID=0x016, DLC=8, data0=0x06`。这说明 CAN 反馈格式和 ID 匹配，但高 4 位为 `0`，电机仍处于未使能状态。已使能时预期 `data0=0x16`。
- 反馈帧数持续增加只能证明主控收到了 CAN 数据，不能证明使能帧已发出或已被电机接受。
- 达妙调试工具截图中配置为 MIT 模式，CAN ID `0x06`，Master ID `0x16`，与工程配置一致。

## 已完成的代码修改

### 真正故障才失能

文件：`H723VGT6-上层/User/Driver/Motor/upper_motor_port.c`

- 增加 J4310 真正故障码范围判断：`0x08..0x0E`。
- 自动失能、故障事件、健康状态和永久零位验证不再把非零状态全部视为故障。
- `0x01` 表示电机已使能，不再因此发送 `0xFD` 失能帧。
- 手动停止和永久标定仍会显式失能，这是保留的安全行为。

### 回归测试

文件：`H723VGT6-上层/User/Tests/test_upper_motor_routing.c`

- 新增反馈状态 `0x01` 的回归测试，验证已使能状态不会触发失能。
- 保留 `0x0B` 真正故障只发一次失能帧的测试。

### 文档

文件：`上层分支/上层代码框架说明.md`

- 已明确只有 `0x08..0x0E` 属于真正故障，`0x01` 为使能状态。

## 已完成验证

- 使用 MinGW GCC 对聚焦的电机路由测试进行编译，选项包含 `-std=c11 -Wall -Wextra -Werror -pedantic`，测试返回 0。
- Keil 构建日志显示 `upper_motor_port.c` 和 `j4310.c` 已编译，结果为 `0 Error(s), 0 Warning(s)`。
- 已生成 `.axf` 和 `.hex`，但编译成功不能证明录屏时运行的固件已刷新为最新版本。

## 录屏分析结论

录屏：`D:\录制视频win+g\屏幕录制 2026-08-14 110113.mp4`

- 录屏时长约 6.7 秒。
- 拖动 J4310 滑块时，“允许 J4310 输出”会短暂勾选；约 1 秒后勾选被清除，诊断显示也短暂重置为 `--`，随后重新出现。
- 这与上位机 `BOARD_RESPONSE_TIMEOUT_MS=1000` 的超时分支吻合：板端状态超过 1 秒未到达时，`_periodic_send()` 调用 `_disable_all_outputs()` 并重新握手，因此界面和勾选状态反复变化。
- 滑块当前在 Tk 每次值变化回调时发送扩展目标帧，同时周期任务每 50 ms 再发，存在串口发送爆发和状态回包处理不及时的风险。

## 已定位的反馈角度问题

- J4310 角度已在驱动层从反馈的 `data[1..2]` 解析，并由板端状态帧传给上位机。
- 当前新鲜度判断使用无符号减法：`(tick_ms - updated_at_ms) <= timeout`。
- 主循环先缓存周期 tick，CAN 中断可能在之后用更新的 tick 写入反馈。此时“旧周期 tick - 新反馈 tick”会无符号下溢，把刚收到的反馈误判为超时，上位机因而显示 `-- deg`。

## 原定修改项（已完成）

1. 修正 J4310 反馈新鲜度的 tick 竞态/无符号下溢，补充“反馈时间戳比周期 tick 新 1 ms”的测试。
2. 限制上位机滑块发送频率：按下时立即发一帧，拖动中只由 50 ms 周期任务发送，释放时再发最终值。
3. 修改“允许 J4310 输出”勾选框行为：勾选时立即发送当前 J4310 目标，从而触发板端先发 `0xFC` 使能帧；取消勾选时显式停止 J4310。
4. 优化 1 秒板端回包超时处理，避免短暂串口拥塞就清空操作员的允许状态和反馈显示。
5. 在板端状态帧增加 J4310 TX 诊断：使能请求次数、发送成功/失败次数、最近 TX CAN ID/DLC/data7，以及软件认为的使能状态。这样可以直接区分“上位机未下发”、“主控未调度”、“FDCAN 入队失败”和“电机未接受”。
6. 在上位机直接显示 J4310 反馈状态：未使能（`0x0`）、已使能（`0x1`）或具体异常/故障（`0x2/0x3/0x5/0x7/0x8..0xE`）。

## 2026-08-14 本次修改进展

- 已找到未使能的核心状态机缺陷：旧代码只要将 `0xFC` 成功放入 HAL TX 队列，就立即认为 J4310 已使能；即使电机仍反馈 `state=0x0`，后续也只发 MIT 而不再重发使能。
- 已改为反馈确认制：`state=0x0` 时每 20 ms 重发 `0xFC`，收到本次使能请求之后的 `state=0x1` 才开始发 MIT。
- 已补充真实故障码 `0x2/0x3/0x5/0x7/0x8..0xE`；`0x0` 是未使能，`0x1` 是已使能。
- 已修复反馈时间戳比当前周期新 1 ms 时的无符号下溢误判。
- 已将滑块拖动发送改为 50 ms 节流，按下和释放仍各发一次。
- 已将“允许 J4310 输出”改为“使能 J4310 输出”，用户勾选时立即下发当前目标，取消时立即下发停止。
- 短暂板端状态回包超时会重新握手，但不再清空 J4310 输出授权；板端 PC 链路超时也不再自动停止 J4310。
- `ROBOT_STATE` 已扩展为 80 字节，新增 J4310 TX 尝试/入队/失败、使能/MIT/失能帧计数、最近 TX ID/DLC/data7、使能确认和反馈状态。

## 最终验证结果

- 电机路由 C 测试使用 `-std=c11 -Wall -Wextra -Werror -pedantic` 编译并运行通过，覆盖使能重试、反馈确认、MIT 切换、`0x02` 异常失能和反馈时间戳竞态。
- 板端 PC 协议 C 测试编译并运行通过，验证 80 字节 `ROBOT_STATE` 的 TX 诊断字段和偏移。
- 上位机完整回归测试共 62 项，全部通过。
- Keil µVision V5.43 / ARMClang V6.24 已完整构建 `H723VGT6.uvprojx`，结果为 `0 Error(s), 0 Warning(s)`，已更新 `MDK-ARM/H723VGT6/H723VGT6.axf` 和 `H723VGT6.hex`。
- 实机验证需将 2026-08-14 11:58 生成的新 HEX 实际烧录到 H723。

## 注意事项

- 当前录屏只有 RX 诊断，尚无法由界面证明 `0x006 / FF FF FF FF FF FF FF FC` 已经进入 FDCAN1 TX 队列。
- 最终确认修复需要将新固件实际下载到 H723，不能只看 Keil 编译时间。

---

# 2026-08-14 E32 LoRa 遥控链路对话上下文

## 用户需求演进

1. 配置接收端工程 `D:\桌面\暑期培训\F103C8T6` 的 CubeMX，引出 GPIO，并配置 SPI、UART。
2. 接收端网表为 `D:\桌面\网表文件\暑期培训\电源\电源接收.tel`。
3. 遥控发送端工程为 `D:\桌面\暑期培训\暑期培训-B组\培训遥控`，网表为 `D:\桌面\网表文件\暑期培训\遥控\遥控.tel`。
4. 接收端定义：H1 是 E32-433T30D，H2 是 SWD 烧录，H3 是 SPI，H4 是 UART，其余 U1/U4 引脚均为普通 GPIO。
5. 发送端采集按键、拨动开关、摇杆和肩键 ADC，通过 E32-433T30D 发送。
6. 用户要求 MCU 与 E32 之间的 UART 使用模块支持的最高波特率，并注意收发同步。
7. 用户询问 CubeMX 的 FreeRTOS/SysTick 警告、RF Setting 的含义和使用方法，以及是否能由 MCU 自主配置 E32 后切换为透明传输。

## 已完成的接收端配置

- CubeMX 文件：`D:\桌面\暑期培训\F103C8T6\F103C8T6.ioc`。
- STM32F103C8T6 使用外部 8 MHz 晶振，系统时钟 72 MHz。
- H1 LoRa：PB0=M0、PB1=M1、PB10=USART3_TX、PB11=USART3_RX、PB12=AUX。
- USART3 配置为 115200、8N1，并启用接收中断。
- H2：PA13=SWDIO、PA14=SWCLK、3V3、GND。
- H3：SPI1，PA5=SCK、PA6=MISO、PA7=MOSI；主机 Mode 0、软件 NSS、4.5 Mbit/s。
- H4：USART2，PA2=TX、PA3=RX，115200、8N1。
- U1/U4 的 MCU 信号脚均配置为普通 GPIO 输入、无上下拉。
- 根据用户最新定义，已从接收端移除 USART1 和 SPI2 占用；PA9、PA10、PB13、PB14、PB15 已恢复为 U4 普通 GPIO。
- LoRa 接收实现：`D:\桌面\暑期培训\F103C8T6\Core\Src\remote_link.c` 和 `Core\Inc\remote_link.h`。
- 接收端使用 USART3 单字节中断解析固定帧，校验 CRC8、协议版本和序号，统计丢帧/CRC/UART 错误。
- 连续 500 ms 没有合法帧判定失联；PC13 LED 表示链路状态。
- 应用层通过 `RemoteLink_GetSnapshot()` 原子读取遥控快照，并必须检查其连接状态返回值。

## 已完成的发送端配置

- CubeMX 文件：`D:\桌面\暑期培训\暑期培训-B组\培训遥控\b-remote control.ioc`。
- STM32F407VET6 的 E32 接口：PD14=M0、PD15=M1、PC6=USART6_TX、PC7=USART6_RX、PC8=AUX。
- USART6 配置为 115200、8N1。
- LoRa 发送实现：`培训遥控\user\Remote.c` 和 `Remote.h`。
- 输入采集保留 ADC DMA、摇杆校准和死区处理；按键/开关改为约 20 ms 消抖。
- 蜂鸣器改成非阻塞截止时间处理，移除运行期阻塞延时。
- 输入任务周期 5 ms，调试任务周期 50 ms，LoRa 发送任务周期 20 ms。
- AUX 为低时跳过当前周期，不缓存旧控制帧，防止空中链路繁忙时形成遥控延迟。
- CubeMX 的 HAL 时基已从 SysTick 改为 TIM6；SysTick 只供 FreeRTOS 使用，解决用户截图中的代码生成警告。

## 遥控通信协议

- 固定 16 字节，每 20 ms 一帧，即 50 Hz。
- 字节 0..1：帧头 `A5 5A`。
- 字节 2：协议版本 `01`。
- 字节 3：8 位循环序号。
- 字节 4..7：左右肩键 ADC，小端序。
- 字节 8..11：左 X、左 Y、右 X、右 Y。
- 字节 12：6 个拨动开关位图。
- 字节 13：KEY1..KEY8 位图。
- 字节 14 bit0..3：KEY9..KEY12；bit7：ADC 故障。
- 字节 15：CRC8，计算字节 0..14，多项式 `0x07`。

## E32-433T30D 参数结论

- USART3 和 USART6 是 MCU 外设编号，不代表速度；收发两端实际都使用 115200、8N1。
- 亿佰特官方规格确认该模块 UART 上限为 115200，空中速率上限为 19.2 kbps，两者属于不同层级。
- 两只模块应设置为：UART 115200/8N1、空速 19.2 kbps、透明传输、信道 23、地址 0、相同 FEC/网络参数。
- E32-433T30D 是 1 W 模块，VCC 建议使用稳定 5V/1A 以上供电；UART 信号使用 3.3V TTL；必须共地并连接 433 MHz 天线。
- 完整接线和参数说明：`D:\桌面\暑期培训\暑期培训-B组\LoRa遥控通信说明.md`。

## RF Setting 使用结论

- RF Setting 是亿佰特 Windows 参数配置工具，并不是运行时通信组件。
- 推荐将 E32 从板上拔下，通过 3.3V 逻辑 USB-TTL 连接：USB-TTL TX->E32 RXD、USB-TTL RX->E32 TXD、GND 共地，E32 VCC 使用独立稳定 5V。
- 配置时 M0=1、M1=1，配置串口使用固定 9600、8N1；先读取参数，再修改并写入，最后再次读取校验。
- 配置完成后断电，令 M0=0、M1=0，重新上电进入透明传输，正常数据 UART 使用 115200、8N1。
- 两只模块需要分别配置，所有无线参数保持一致。
- 读取失败时检查 COM 口、驱动、RX/TX 交叉、共地、M0/M1 高电平，以及模块是否仍与正在运行的 MCU 发生引脚冲突。

## MCU 自动配置的当前状态

- 硬件已经具备 MCU 自动配置条件：两端的 M0、M1、AUX 和 UART 都接到了 MCU。
- 当前固件只在初始化时设置 M0=0、M1=0 并进入透明传输，尚未自动改写 E32 内部参数。
- 自动配置可采用：进入配置模式 -> MCU UART 临时切换 9600/8N1 -> 写入并回读参数 -> 等待 AUX -> 切回 M0=M1=0 -> MCU UART 切换 115200 -> 开始透传。
- 同名 E32-433T30D 存在固件协议差异：新版官方资料使用 AT 指令，旧版常见固件使用 `C0/C1` 六字节二进制指令。
- 在未确认实体模块协议版本前，不应盲目固定发送某一种写参数命令。后续实现应先识别版本，读取比较，仅在参数不一致时写入；配置失败则保持安全状态，不启动遥控发送或控制执行。
- 需要用户提供模块正反面清晰照片、标签/固件版本或购买批次，才能选择并实机验证准确的自动配置协议。

## 构建与产物

- 接收端使用 Keil ARMCC V5.06 update 7 全量构建，结果 `0 Error(s), 0 Warning(s)`。
- 接收端 HEX：`D:\桌面\暑期培训\F103C8T6\MDK-ARM\F103C8T6\F103C8T6.hex`，生成时间 2026-08-14 12:05:17。
- 发送端使用 Keil ARMCC V5.06 update 7 全量构建，结果 `0 Error(s), 0 Warning(s)`。
- 发送端 HEX：`D:\桌面\暑期培训\暑期培训-B组\培训遥控\MDK-ARM\b-remote control\b-remote control.hex`，生成时间 2026-08-14 11:57:51。

---

# 2026-08-14 E32 LoRa MCU 上电自动配置

- 发送端：`培训遥控/user/Remote.c`、`Remote.h`。
- 接收端：`D:\桌面\暑期培训\F103C8T6\Core\Src\remote_link.c`、`Core\Inc\remote_link.h`。
- 两端上电后先进入 E32 配置模式（M0=M1=1），MCU UART 临时切换到 9600/8N1。
- 使用 E32 六字节寄存器协议读取当前参数；仅在不一致时写入 `C0 00 00 3D 17 44`，然后再次回读校验。
- 参数含义：地址 0、UART 115200/8N1、空中速率 19.2 kbps、透明传输、信道 23、FEC 开启、推挽 IO、30 dBm。
- 回读完全一致且 AUX 就绪后，MCU UART 才切回 115200，M0=M1=0 进入透明模式。
- 发送端未配置成功时禁止发送遥控帧；接收端未配置成功时不启动 USART3 中断接收。
- 两端均保留配置状态、尝试次数和最后六字节回读值，便于实机调试。
- F407 发送端和 F103 接收端均已使用 Keil ARMCC V5.06 update 7 全量构建，结果均为 `0 Error(s), 0 Warning(s)`。
- 新发送端 HEX 时间为 2026-08-14 14:39:46；新接收端 HEX 时间为 2026-08-14 14:40:17。

---

# 2026-08-14 J4310 断电重启回零开关

- 读取本日志最新内容后，在 `H723VGT6-上层` 增加 J4310 自动回零状态机：首次有效反馈只建立基准；检测到 J4310 反馈断开、随后恢复时，从断电前位置以五次多项式轨迹平滑回到零位并保持。
- 上位机 `上位机/main.py` 在 J4310 操作区增加“重启归零：开启/关闭”开关。设置通过 `PC_MSG_MOTOR_ACTION` 动作 `0x02` 写入 W25Q 外部 Flash，掉电后仍保留；Flash 未就绪时按钮禁用。
- 新增状态回传尾字段（状态载荷 84 bytes）：存储是否可用、开关状态、当前是否正在回零、状态阶段。J4310 普通目标、显式停止、急停或永久标定会释放自动回零控制。
- 为避免 J4310 回零期间被普通停止帧误判为无关目标，协议使能掩码 bit6 作为 J4310 显式停止标志；M3508/M2006 的普通控制格式保持不变。
- 新增 `User/Application/Robot/j4310_auto_return.c/.h` 和对应单元测试；更新上位机协议测试、板端通信测试及电机路由测试。

## 本次验证

- `test_j4310_auto_return.c`：通过。
- `test_upper_pc_link.c`：通过。
- `test_upper_motor_routing.c`：通过。
- 上位机 `python -m py_compile main.py protocol.py`：通过。
- 上位机 `python -m unittest test_console.py`：64 tests，全部通过。
- Keil ARMClang V6.24 全量构建：`0 Error(s), 0 Warning(s)`；新 HEX 为 `D:\桌面\暑期培训\暑期培训-B组\上层分支\H723VGT6-上层\MDK-ARM\H723VGT6\H723VGT6.hex`。

## 实机使用

- 先将上述 HEX 烧录到 H723，再启动上位机并完成握手；Flash 初始化完成后，J4310 旁的开关显示当前持久化状态。
- 打开开关不会立即让电机运动；需要经历一次有效反馈建立基准，之后下电、手动转动 J4310、重新上电并恢复通信，固件才会自动平滑回零。
- 本次只完成固件和上位机逻辑，尚未替用户执行实际烧录、断电和机械动作测试。

# 2026-08-14 F103 USART2 ASCII 与 PC13 接收指示

- F103 `Core/Src/remote_link.c` 新增 `RemoteLink_SendAsciiStatus()`，主循环每 50 ms 通过 USART2（PA2 TX，115200/8N1）输出 ASCII 状态。
- `LORA OK` 行包含四路摇杆、两个肩键 ADC、SW1..SW6、KEY1..KEY12、ADC_ERR 和序号；配置失败/未收到合法帧时输出对应诊断行。
- USB 转 TTL 应接 F103 PA2（TTL RX）和 GND，不需要连接 USB 转 TTL TX。
- 每收到一帧 CRC 正确的 LoRa 遥控帧，PC13 LED 点亮 5 ms 后熄灭；PC13 电平和 LED2 连接关系已由 `电源接收.tel` 复核。
- F103 最新 HEX 构建时间为 2026-08-14 15:25:41，Keil ARMCC 结果为 `0 Error(s), 0 Warning(s)`。

# 2026-08-14 E32 status=7 实机修正

- 实机持续输出 `status=7 readback=C0 00 06 3C 06 44`，证明读取成功但写入后校验失败。
- 原因是实物为旧版 E32 参数编码：`SPED=0x3C` 才表示 UART 115200/8N1、空中速率 19.2 kbps；先前按新版编码使用的 `0x3D` 对该模块无效，模块因此拒绝整帧配置。
- 两端目标配置已统一修正为 `C0 00 00 3C 17 44`。重新烧录后应先写入一次，随后状态变为 2，回读变为同一目标值。
- PC13 只在接收端进入正常模式并收到 CRC 正确的遥控帧后闪烁；status=7 时发送和接收均被安全门控禁止。

## 2026-08-14 E32 10408-V1.1 最终构建确认

- `10408-V1.1` 是模块 PCB 版本标记，不能单独确定 SPED 寄存器编码；本次以实物能够回读 `C0 00 06 3C 06 44`、但拒绝含 `0x3D` 的写入作为判定依据。
- F407 发送端和 F103 接收端目标配置均为 `C0 00 00 3C 17 44`，源码中已无 `0x3D` 目标配置残留。
- F407 发送端使用 Keil ARMCC V5.06 update 7 全量构建，结果为 `0 Error(s), 0 Warning(s)`；HEX 时间为 2026-08-14 15:36:00。
- F103 接收端使用 Keil ARMCC V5.06 update 7 全量构建，结果为 `0 Error(s), 0 Warning(s)`；HEX 时间为 2026-08-14 15:44:48。
- 必须把上述两个最新 HEX 分别烧录到发送端和接收端。仅更新一端会使地址/信道参数不一致，无法收到有效遥控帧，也不会触发 PC13 脉冲。

## 2026-08-14 当前对话保存：LoRa 实机故障与处理状态

### 用户反馈与最新要求

- F103 接收端通过 USART2 持续输出：`LORA CONFIG status=7 attempts=3 readback=C0 00 06 3C 06 44`。
- 按下遥控器按键时，接收端 PC13 不闪烁。
- 用户补充 LoRa 模块 PCB 丝印为 `10408-V1.1`，随后要求继续处理，并要求先保存本轮对话。

### 状态含义

- `status=7` 对应 `REMOTE_LINK_LORA_CONFIG_VERIFY_ERROR`，表示 MCU 能够使用 `C1 C1 C1` 读到模块参数，但写入目标参数后再次读取时，结果仍未与目标完全一致。
- 配置失败时，发送端会禁止发送遥控帧，接收端也不会启动 USART3 正常接收，因此 USART2 会每 50 ms 重复输出配置错误行，PC13 也不会闪烁。这两个现象属于同一个配置失败问题。
- `10408-V1.1` 只能作为 PCB 版本线索，不能单独确定模块频段、产品型号或 SPED 编码；仍应以模块完整标签、版本查询结果和实机读写行为为准。

### 当前代码状态

- F407 发送端 `培训遥控/user/Remote.c` 与 F103 接收端 `Core/Src/remote_link.c` 当前目标配置均为 `C0 00 00 3C 17 44`。
- 两端均在 MCU 上电后先令 `M0=M1=1`，将 LoRa UART 临时切换到 9600/8N1，读取、按需写入并回读校验配置；成功后切回 UART 115200/8N1 和透明传输模式，再开始收发遥控帧。
- F103 已通过 USART2 PA2 输出 ASCII 遥控状态，包含 `LX/LY/RX/RY`、`SHL/SHR`、`SW1..SW6`、`KEY1..KEY12`、`ADC_ERR`、序号及链路诊断计数。
- F103 每提交一帧 CRC 正确的 16 字节遥控帧，就将 PC13 置高 5 ms，再由主循环非阻塞熄灭；无效帧和配置失败不会触发 LED。

### 构建产物

- F407 发送端 Keil ARMCC V5.06 update 7 全量构建通过：`0 Error(s), 0 Warning(s)`。
- 发送端 HEX：`D:\桌面\暑期培训\暑期培训-B组\培训遥控\MDK-ARM\b-remote control\b-remote control.hex`，生成时间 2026-08-14 15:36:00。
- F103 接收端 Keil ARMCC V5.06 update 7 全量构建通过：`0 Error(s), 0 Warning(s)`。
- 接收端 HEX：`D:\桌面\暑期培训\F103C8T6\MDK-ARM\F103C8T6\F103C8T6.hex`，生成时间 2026-08-14 15:44:48。

### 下一步实机验证

- 必须分别烧录上述最新发送端和接收端 HEX，然后让两端重新上电；不能只更新一端。
- 若 `0x3C` 配置被实物接受，接收端应先显示 `LORA WAIT ...`，收到有效帧后显示 `LORA OK ...`，同时 PC13 按有效数据包产生 5 ms 脉冲。
- 当前官方 E32-T30x 手册将 SPED 低三位 `100`（即这里的 `0x3C`）定义为空中速率 9.6 kbps，将 `101`（`0x3D`）定义为 19.2 kbps；但本次实物在目标包含 `0x3D` 时未通过回读校验。因此当前先采用实物已有且可回读的 `0x3C` 作为兼容配置，通信恢复后如仍要求 19.2 kbps，需要结合模块完整型号/版本查询和写命令响应继续确认，不能仅凭 `10408-V1.1` 下结论。
- 本轮只完成源码、文档和 Keil 构建，尚未代替用户执行实际烧录与射频收发验证。

---

# 2026-08-14 LORA status=7 处理结果（本轮）

- 对照官方 E32 旧版寄存器定义确认：配置模式固定使用 `9600/8N1`，因此 MCU 临时切换串口速率的流程没有问题；`SPED=0x3C` 表示 `115200/8N1 + 9.6 kbps`，`0x3D` 才表示 19.2 kbps。
- 实机连续回读 `C0 00 06 3C 06 44`，说明模块已经保存了一组有效参数，但拒绝固件强制写入 `C0 00 00 3C 17 44`。继续强制写入会使两端始终停在 `status=7` 安全门控，发送端不发帧、接收端不收帧。
- 已将 F407 发送端 `培训遥控/user/Remote.c` 和 F103 接收端 `D:\桌面\暑期培训\F103C8T6\Core\Src\remote_link.c` 的兼容目标统一为 `C0 00 06 3C 06 44`。该配置与实机当前回读值完全一致，上电时应直接通过校验，不再执行失败写入。
- 接收端 `LORA CONFIG` 调试行已附带 `expected=C0 00 06 3C 06 44`，用于确认烧录的是本次新固件。
- 已同步更新 `LoRa遥控通信说明.md`，更正 `0x3C` 的空速含义和地址/信道说明。
- F407 发送端已使用 Keil ARMCC V5.06 update 7 全量重建，结果 `0 Error(s), 0 Warning(s)`；新 HEX 时间 `2026-08-14 16:09:06`，SHA-256 为 `64E10D826467C19F4D6DA099CC8AAD33082E7B1AAEEBC6DF1702296D6BF6B06B`。
- F103 接收端已使用 Keil ARMCC V5.06 update 7 全量重建，结果 `0 Error(s), 0 Warning(s)`；新 HEX 时间 `2026-08-14 16:08:57`，SHA-256 为 `ECD8FD0EC1FC1D034665938FDC60C6ADB22713CC9A8D1B9CEC4B0BFADDC64CF1`。
- 已检查两个 AXF，均包含 `C0 00 06 3C 06 44`；F103 AXF 同时包含新增的 `expected=C0 00 06 3C 06 44` 调试文本，确认新配置已经进入构建产物。
- 下一步必须分别烧录本次重新构建的 F407、F103 HEX；预期接收端先输出 `LORA WAIT`，收到帧后输出 `LORA OK` 并使 PC13 闪烁。若仍显示旧的 `expected` 或 `status=7`，说明实际烧录的不是新 HEX，或两只模块的当前回读参数并不相同。

---

# 2026-08-14 LORA WAIT valid=0 后续处理

- 用户烧录兼容配置固件后，F103 持续输出 `LORA WAIT valid=0 crc_errors=0 uart_errors=0`。这证明接收端已通过 E32 配置并进入 USART3 正常接收状态，原 `status=7` 已解决；当前没有收到完整遥控帧。
- 发送端 FreeRTOS 的 `loraTask` 已确认每 20 ms 调用 `Remote_SendLoRa()`。新的首要嫌疑是发送端 E32 地址与接收端不同，而旧代码错误地对六字节配置做全量比较，使透明模式下本应无关的地址差异也能阻止发送。
- 两端配置匹配已改为只检查 `HEAD/SPED/CHAN/OPTION`，即要求 `C0 XX XX 3C 06 44`；`ADDH/ADDL` 不参与透明模式校验。需要改写无线参数时也会保留模块当前地址。
- 发送端 USART1 调试已从 VOFA JustFloat 二进制改为 ASCII：`LORA TX ready/status/attempts/readback/frames/busy/errors/cfgskip/aux`。该串口连接遥控器板载 USB1/CH340，设置 `115200/8N1` 即可直接观察发送端是否进入 ready 及发送帧数是否增长。
- F407 发送端全量构建通过：`0 Error(s), 0 Warning(s)`；新 HEX 时间 `2026-08-14 16:25:43`，SHA-256 `460B602C66A18972F0CB0F221EC7AB01A1ED7EE5C804F75D6FA8723833477664`。
- F103 接收端全量构建通过：`0 Error(s), 0 Warning(s)`；新 HEX 时间 `2026-08-14 16:25:38`，SHA-256 `FBFE26EDD25F1767A28C2CDC65F5D326E53A13A6BDE42EB1D378F9E092E34E89`。
- 已核对 AXF：F407 包含 `LORA TX ready=` 新诊断字符串，F103 包含 `expected=C0 XX XX 3C 06 44`，确认本轮修改已进入固件产物。

---

# 2026-08-14 E32 RF Setting V4.6 参数确认

- 用户提供 RF Setting V4.6 截图，确认发送端和接收端均应使用：模块地址 `6`、UART `115200/8N1`、空中速率 `9.6 kbps`、FEC 开启、透明传输、唤醒时间 `250 ms`、推挽 IO、发射功率 `30 dBm`、信道 `31`。
- 截图当前参数为 `0x0, 0x6, 0x3c, 0x1f, 0x44`，对应六字节寄存器帧 `C0 00 06 3C 1F 44`；频率显示为 `441.0 MHz`。
- 已将 F407 `培训遥控/user/Remote.c` 和 F103 `D:\桌面\暑期培训\F103C8T6\Core\Src\remote_link.c` 的目标信道从 `0x06` 改为 `0x1F`，同时保留透明模式下忽略地址的匹配逻辑。
- 接收端配置失败诊断行的新目标为 `expected=C0 XX XX 3C 1F 44`。两端必须重新构建并分别烧录新 HEX，旧 HEX 仍会按信道 6 工作。
- F407 发送端按信道 31 全量构建通过：`0 Error(s), 0 Warning(s)`；HEX 时间 `2026-08-14 17:06:22`，SHA-256 `FEC8EFDA1974BE11FDBD7986C519BD95F24CEAA57FB9AF7B0D001B6072124687`。
- F103 接收端按信道 31 全量构建通过：`0 Error(s), 0 Warning(s)`；HEX 时间 `2026-08-14 17:07:25`，SHA-256 `E4BF398538F12CE8013C4DDC32015645A76A6E8FB473340F0661BBC3C5EB510A`。
- 已在两个 AXF 中搜索到完整配置字节序列 `C0 00 06 3C 1F 44`，确认信道 31 已进入最终固件。

---

# 2026-08-14 撤销信道 31 修改

- 用户要求取消上一轮根据 RF Setting 截图执行的信道 31 修改。
- 已恢复发送端和接收端目标为 `C0 XX XX 3C 06 44`，即信道 6；透明模式地址忽略和发送端 ASCII 诊断保持不变。
- 上一轮信道 31 HEX 不再作为当前烧录版本；恢复后的两端需要重新构建并分别烧录。
- 恢复后的 F407 全量构建通过：`0 Error(s), 0 Warning(s)`；HEX 时间 `2026-08-14 17:13:58`，SHA-256 `460B602C66A18972F0CB0F221EC7AB01A1ED7EE5C804F75D6FA8723833477664`。
- 恢复后的 F103 全量构建通过：`0 Error(s), 0 Warning(s)`；HEX 时间 `2026-08-14 17:13:53`，SHA-256 `1FB1967CF880399ED807973259C7D337051DD36386A994B8CB5229DA5F9B9747`。
- 两个 AXF 均包含 `C0 00 06 3C 06 44`，且均不再包含完整的 `C0 00 06 3C 1F 44` 配置序列。

---

# 2026-08-14 恢复此前通信配置并增强收发诊断

- 按用户要求恢复日志最初的 E32 配置 `C0 00 00 3C 17 44`：地址 0、UART 115200/8N1、空速 9.6 kbps、信道 23、透明传输、FEC 开启、推挽、30 dBm。
- 发送端和接收端均改为六字节全量比较；不再忽略地址，也不再保留模块当前地址，确保两端回读配置完全相同。
- 接收端 USART3 接收重装载改为检查 `HAL_UART_Receive_IT()` 返回值，并增加 `rx_bytes`、`rx_cb`、`arm_errors` 诊断计数。
- 发送端增加 `attempts_tx`，用于区分 LoRa 任务未运行、配置跳过、AUX 忙和 UART 发送失败。
- PC13 硬件为高电平点亮：网表中 LED2 的阳极接 STM32 PC13，阴极经 R2 接地；已将合法帧指示脉冲从 5 ms 调整为 50 ms，便于肉眼观察。
- F407 发送端全量构建通过：`0 Error(s), 0 Warning(s)`；HEX 时间 `2026-08-14 19:29:41`，SHA-256 `BBB9D4DDFB663FCDC04FB900AC8C7D301CED397DD0987CA995253891D20BD6A7`。
- F103 接收端全量构建通过：`0 Error(s), 0 Warning(s)`；HEX 时间 `2026-08-14 19:29:33`，SHA-256 `AF186C7CAEDC38DED3A6EACFEABD8497A6281889AA27DF48ADFFE2B5961318D1`。
- 两个 AXF 均搜索到完整配置 `C0 00 00 3C 17 44`；F407 包含 `attempts_tx=`，F103 包含 `rx_bytes=` 和 `expected=C0 00 00 3C 17 44`，确认配置和诊断均进入固件。

---

# 2026-08-14 正式 LoRa 通信恢复到实机可用配置

- 对照 `D:\桌面\u-remote control` 与 `D:\桌面\lora` 确认：旧工程使用 USART6/USART3 的 115200/8N1 透明传输，模块依赖 EEPROM 中已保存的 `C0 00 06 3C 06 44`；旧工程仅用单字节 `0xA5` 验证链路。
- 正式工程不采用单字节测试协议，继续使用 16 字节 `A5 5A` 遥控帧、版本、序号和 CRC8。
- F407 发送端和 F103 接收端的目标配置统一改为 `C0 00 06 3C 06 44`，并对六个字节完全比较，确保地址、UART/空速、信道和 OPTION 全部相同。
- 配置失败时两端都会将 M0/M1 恢复为低并把 LoRa UART 恢复到 115200，避免 E32 一直停留在配置模式；但 `ready` 仍保持为 0，不会在配置不确定时发送或执行遥控数据。
- F407 发送端全量构建通过：`0 Error(s), 0 Warning(s)`；HEX 时间 `2026-08-14 19:50:55`，SHA-256 `C5AF72E4995ED07AB3010CB8511739141ECDFB5D082420E8EEC91A6A4BF2120D`。
- F103 接收端全量构建通过：`0 Error(s), 0 Warning(s)`；HEX 时间 `2026-08-14 19:49:35`，SHA-256 `081D8BD772924B46F876D0D5A59AD6FC21FA990C6FA019F5C9B79EF9CD9589E0`。
- 两个 AXF 均已扫描到完整配置字节；F407 同时包含 `attempts_tx=`，F103 同时包含 `expected=C0 00 06 3C 06 44` 和 `rx_bytes=`，确认修改进入最终固件。

---

# 2026-08-14 LoRa 空中速率提升到 19.2 kbps

- 按用户要求将正式 LoRa 链路的空中速率由 9.6 kbps 提升到 19.2 kbps。
- F407 发送端与 F103 接收端目标配置同步改为 `C0 00 06 3D 06 44`；地址 6、信道 6、UART 115200/8N1、透明传输和 OPTION `0x44` 保持不变。
- 正式 16 字节遥控帧、20 ms 发送周期、序号和 CRC8 均保持不变。
- F407 发送端全量构建通过：`0 Error(s), 0 Warning(s)`；HEX 时间 `2026-08-14 20:05:33`，SHA-256 `F5DDC6B86CCCA0D8C06244BC248129499480C4917D97E9952BDA8D9A1EB961E9`。
- F103 接收端全量构建通过：`0 Error(s), 0 Warning(s)`；HEX 时间 `2026-08-14 20:05:28`，SHA-256 `84DD807250E01B4E5F0EA5AD7FCF1D4CA6515BB938F1275C84AE0172CDCAF6CB`。
- 两个 AXF 均扫描到完整配置 `C0 00 06 3D 06 44`，F103 AXF 同时包含 `expected=C0 00 06 3D 06 44`。

---

# 2026-08-14 撤销 19.2 kbps 空中速率

- 实机烧录后持续显示 `status=7 readback=C0 00 06 3C 06 44 expected=C0 00 06 3D 06 44`，确认模块拒绝保存 `SPED=0x3D`。
- 按用户要求将 F407 发送端和 F103 接收端恢复为已验证可通信的 `C0 00 06 3C 06 44`，即 9.6 kbps 空中速率。
- 正式 16 字节遥控帧、20 ms 周期、地址 6、信道 6、CRC8 和透明传输均保持不变。
- F407 恢复版全量构建通过：`0 Error(s), 0 Warning(s)`；HEX 时间 `2026-08-14 20:10:45`，SHA-256 `C5AF72E4995ED07AB3010CB8511739141ECDFB5D082420E8EEC91A6A4BF2120D`。
- F103 恢复版全量构建通过：`0 Error(s), 0 Warning(s)`；HEX 时间 `2026-08-14 20:10:40`，SHA-256 `081D8BD772924B46F876D0D5A59AD6FC21FA990C6FA019F5C9B79EF9CD9589E0`。
- 两个 AXF 均包含完整 `C0 00 06 3C 06 44`，且不再包含完整 `C0 00 06 3D 06 44` 目标序列。

---

# 2026-08-14 降低 LoRa 发送频率并修改 PC13 事件指示

- 正式 16 字节遥控帧发送周期由 20 ms（50 Hz）降低为 50 ms（20 Hz），减轻 9.6 kbps 空中链路与 AUX 的持续负载。
- F407 LoRa 任务改用 `osDelayUntil()` 绝对周期调度，避免发送执行时间累积到下一周期。
- F103 PC13 不再由每个合法帧触发；第一帧只建立状态基准，随后 KEY1..KEY12 任一按键出现按下沿，或 SW1..SW6 任一拨动开关状态变化时，PC13 点亮 100 ms 后熄灭一次。
- 按键松开、摇杆/肩键 ADC 变化和普通连续收帧不会触发 PC13。
- F407 20 Hz 发送版全量构建通过：`0 Error(s), 0 Warning(s)`；HEX 时间 `2026-08-14 20:18:00`，SHA-256 `FE18E726D438CF12995F63377890D76054683D3030C2E7ADDBA065F94AEC578D`。
- F103 事件 LED 版全量构建通过：`0 Error(s), 0 Warning(s)`；HEX 时间 `2026-08-14 20:17:54`，SHA-256 `49D6E66038D320665EEC66A634FF805FC03F6391D77862138D44A5C42289846B`。

---

# 2026-08-14 发送端 VOFA+ 输出精简为曲线数据

- F407 板载 CH340/USART1 从 LoRa ASCII 诊断改回 VOFA+ `JustFloat` 二进制曲线输出，周期保持 50 ms。
- 仅输出 17 个 float 通道：`LORA_OK`、KEY1..KEY12、左 X/Y 和右 X/Y 四路摇杆原始 ADC；不再输出配置、计数、AUX、错误、拨动开关或肩键数据。
- `LORA_OK=1` 表示发送端 LoRa 配置完成且至少成功发送过一帧；E32 透明传输没有接收端 ACK，因此该通道不代表接收端确认。
- F407 VOFA 精简版全量构建通过：`0 Error(s), 0 Warning(s)`；HEX 时间 `2026-08-14 20:35:27`，SHA-256 `18F010DF244A2149B92A03032162FA7B43D88F637B9E80E9007B5BF61E2D0B53`。
- AXF 已确认不再包含旧的 `LORA TX ready=` ASCII 诊断字符串。

---

# 2026-08-14 接收端拨动开关输出映射调整

- 接收端输出映射改为：遥控器 `PE4/SW1 -> PB14`、`PE3/SW2 -> PA8`、`PE1/SW3 -> PA10`、`PD5/SW6 -> PA12`。
- 原 `PE0/SW4`、`PD6/SW5` 不再控制这四个输出。
- `PA12/U4_6` 已从 GPIO 输入改为推挽输出，复位初始低电平，并同步更新 `F103C8T6.ioc`。

---

# 2026-08-14 USART2 改为转发 LoRa 原始帧

- F103 不再通过 USART2 输出 `LORA OK/LORA WAIT/LORA CONFIG` ASCII，也不输出 VOFA/JustFloat 数据。
- USART3 收到以 `A5 5A` 开头的完整 16 字节 LoRa 帧后，保留原始字节并由主循环通过 USART2 原样发送给电脑；转发在 CRC 判断之前排队，因此电脑可以自行校验原始帧。
- USART3 中断只复制帧，USART2 阻塞发送放在主循环，避免在接收中断内等待串口。
- 发送端 FreeRTOS 调试任务中的 `MyMain_Debug()` VOFA 调用已注释停用，电脑数据统一从接收端 USART2 获取。
- F407 发送端全量构建通过：`0 Error(s), 0 Warning(s)`；HEX 时间 `2026-08-14 20:50:59`，SHA-256 `58B8B4FA242F7A67CF3E55BE1DCC584F212C74DD8CAFE2D691E939A2D2AC80C8`。
- F103 接收端全量构建通过：`0 Error(s), 0 Warning(s)`；HEX 时间 `2026-08-14 20:50:50`，SHA-256 `F85A8182499E811C6B7CE0E20173A6B1B292BC7A1B07D3630793074D2600EF6B`。
- 已扫描 F103 AXF，确认不再包含 `LORA OK`、`LORA WAIT`、`LORA CONFIG` 旧状态字符串。

---

# 2026-08-14 PD5 同时控制 PA11/PA12

- 最终输出映射为：遥控器 `PE4/SW1 -> PB14`、`PE3/SW2 -> PA8`、`PE1/SW3 -> PA10`、`PD5/SW6 -> PA11+PA12`。
- 检测到 `PD5/SW6` 稳定状态相对上一帧变化时，接收端 PA11 和 PA12 同时翻转一次。
- `PB14`、`PA8`、`PA10`、`PA11`、`PA12` 均配置为推挽输出，上电初始低电平；第一帧只建立拨动开关状态基准，不触发翻转。
- F103 全量构建通过：`0 Error(s), 0 Warning(s)`；HEX 时间 `2026-08-14 21:07:33`，SHA-256 `8EABE27D738B0DC5C956C111833B29A102E9319110B33A8BAE930F2BBEAAC463`。
