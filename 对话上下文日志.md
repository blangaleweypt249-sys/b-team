# 上层项目对话上下文日志

本文件专门用于跨对话保存“上层”项目的必要上下文。记录按时间倒序排列，最上面一条记录为最新状态。

## 使用约定

- 用户说“保存日志”时，将当前与上层项目有关的有效对话压缩为一条带时间戳的记录，并插入到已有记录之前。
- 用户说“提取日志”“看一下日志”或要求读取本文件时，先读取最上面的最新记录，再根据其中的状态继续工作；只有需要追溯决策时才读取更早记录。
- 摘要保留：当前目标、已完成事项、关键决定、重要路径、代码改动、未完成事项、下一步和必要的验证结果。
- 摘要省略：寒暄、重复内容、冗长命令输出，以及密码、令牌等敏感信息。
- 本文件只记录 `D:\桌面\暑期培训\暑期培训\上层` 范围内的工作，不混入总工作区或“下层”项目的上下文。
- 每次新增记录时，不覆盖、删除或改写以前的记录。

---

## 2026-08-04 19:25:03 +08:00

### 最新硬件状态修正

- `R34` 已经从实物上去掉，CH340N `RTS#` 不再连接或影响 MCU `BOOT0`。
- 后续定位 DAP 下载成功但 HW RESET 后不运行的问题时，排除 `R34/RTS#` 影响，优先检查 U1.73 的 VCAP2 电容、NRST 波形，以及复位后 PC 停留地址。

---

## 2026-08-04 19:09:27 +08:00

### 当前目标

定位 STM32H723VGT6 上层主控板的冷启动、BOOT0、VCAP 和 ST-Link 下载后复位问题，并确认板载 USB/CH340N 的实际能力。

### 相关文件与工程

- 主控网表：`D:\桌面\网表文件\暑期培训\主控\H723VGT6.tel`。
- 当前 Keil 工程：`D:\桌面\暑期培训\暑期培训-B组\上层\H723VGT6-上层\MDK-ARM\H723VGT6.uvprojx`。
- Keil 工程定义了 `USE_PWR_LDO_SUPPLY`，目标芯片为 `STM32H723xx`。

### 已确认的硬件连接

- 板载 USB-C 的 D+/D- 接入 CH340N，不直接接 STM32H723 的原生 USB 引脚，因此当前只能作为 USB 转串口和供电，不能直接作为 MCU USB-CDC 或 USB DFU 下载口。
- CH340N 的串口接到 MCU `PA0/PA1`，对应 UART4；STM32H723 ROM Bootloader 不支持通过 UART4 下载。
- CH340N `RTS#` 经 `R34=1k` 接到 `BOOT0`，会与 BOOT0 下拉竞争，建议去掉或不焊 R34。
- BOOT0 原电路为 `3V3 -> C13 100nF -> 节点 -> R4 -> BOOT0`，节点再经 R3 接地，SW2 按下时把节点接到 3V3。即使按键没有按下，C13 仍会在 3V3 上升时向 BOOT0 耦合高脉冲。
- C13 已去掉。去掉后重新断电上电能够进入主程序，说明此前的冷启动异常主要来自 BOOT0 上电瞬态，而不是单独由 VCAP 引起。
- `H2-11=BOOT0` 与 `H2-23=GND` 是两个独立焊盘；插跳线帽后属于 BOOT0 直接强制接地，不是电阻下拉。实际下拉路径为 `BOOT0 -> R4 -> R3 -> GND`，但 R3/R4 在网表中的值均为 `{Value}`，需要实测或查 BOM。
- MCU `U1.48 VCAP1` 已经通过 `C3=2.2uF` 接地。
- MCU `U1.73 VCAP2` 在原网表中被错误放入 GND 网络；实物目前已改为悬空，但悬空仍不符合 LDO 模式要求。必须断开直接 GND 后改为 `U1.73 -> 独立 2.2uF 低 ESR 陶瓷电容 -> GND`，电容靠近引脚。
- NRST 网络为 `U1.14/H10.10/C12.2/R17.2/SW3`；C12 为 100nF，R17 从 3V3 上拉，但网表中 R17 阻值为 `{Value}`，需要确认实物阻值和焊接。

### Keil 与 ST-Link 现象

- 原先仅连接 SWDIO、SWCLK、电源和 GND，使用 `SYSRESETREQ`；曾出现 Flash 下载后未自动进入主程序，但手动 NRST 或重新上电能够运行。
- 后来把 ST-Link 的 RST 接到板载 NRST 并选择 `HW RESET`。工程重新编译成功：`0 Error(s), 0 Warning(s)`。
- 在 `HW RESET` 下下载日志显示 `Erase Done`、`Programming Done`，但 Verify 从 `0x08000000` 开始全部读到 `FF`，最终报 `Error: Flash Download failed - "Cortex-M7"`。
- 该日志说明这一次不是“程序写入成功但没有复位”，而是 Flash 被擦除后程序没有可靠写入；当前 Flash 可能为空，需要用能够通过 Verify 的方式重新下载。
- 用户最新反馈 `SYSRESETREQ` 不出现上述问题，而 `HW RESET` 出现问题。这个 A/B 对比优先指向 NRST 物理链路、ST-Link RST 引脚或复位时序，而不是编译、链接或 C13。
- `SYSRESETREQ` 通过 SWD 写系统复位请求，不使用物理 NRST；`HW RESET` 必须由 ST-Link 实际拉低并释放 NRST。如果接错调试器 RST 引脚、H10 双排针编号看反、R17 缺失/阻值过大、NRST 被持续拉低、RST 输出不可靠或复位后延时为 0，都可能造成只在 HW RESET 下失败。
- 第二个 VCAP 悬空仍是必须修复的硬件错误，会降低内核和 Flash 编程稳定裕量；但若其他条件完全相同且 SYSRESETREQ 稳定通过 Verify，仅 HW RESET 失败，则 VCAP 不能单独解释这种模式差异。

### 当前建议配置与排查顺序

1. 先为 U1.73 补独立 2.2uF VCAP 电容，确认该引脚不再直接接地，也不再悬空。
2. 暂时使用 `SYSRESETREQ` 重新下载并确认 Verify 成功，以恢复已被擦除的主程序。
3. 断电测量 `H10.10` 与 `U1.14` 应接近 0Ω，确认双排针物理编号没有看反；同时确认所用 ST-Link 的 `RST` 是目标复位输出，而不是调试器自身复位输入。
4. ST-Link 未接 RST 时、以及接上 RST 后的空闲状态，NRST 都应稳定接近 3.3V；如果接上后变为 0V 或明显偏低，停止使用 HW RESET 并检查接线、调试器和 R17。
5. 确认 R17 实物约为 10k 上拉，C12 为 100nF，板载复位按钮没有漏电或持续导通。
6. 再测试 HW RESET 时，把 SWD 时钟先降至 100~400kHz，`Delay after reset` 设为 100ms，保持 `Stop after Reset` 不勾选。
7. 可暂时取消 `Reset after Connect` 做定位：若 Verify 恢复但最后仍不运行，说明故障集中在最后一次硬件复位；若仍读到全 FF，说明只要 ST-Link 操作 NRST，复位线就没有正确释放或 Flash 算法运行不稳定。
8. 接上 RST 并不强制必须使用 HW RESET。若修复 VCAP 后 `SYSRESETREQ + Reset and Run` 能稳定下载并运行，可以继续使用 SYSRESETREQ；只有确认 NRST 波形和电平正常后才使用 HW RESET。

### 未完成事项

- 为 U1.73 安装 2.2uF VCAP 电容并复测。
- 确认 R3、R4、R17 的实际阻值，以及 R34 是否已经去掉。
- 测量 ST-Link 接入前后 NRST 的空闲电压，并确认 H10.10 到 U1.14 的导通关系。
- 在 VCAP 修复后分别用 SYSRESETREQ 和 HW RESET 下载，对比 Verify 和下载后运行结果。

---

## 2026-07-31 15:01:45 +08:00

### 当前目标

在 `D:\桌面\暑期培训\暑期培训\上层` 建立独立的对话上下文日志，用于后续保存和提取上层项目的工作状态。

### 已完成事项

- 新建 `D:\桌面\暑期培训\暑期培训\上层\对话上下文日志.md`。
- 建立“保存日志”和“提取日志”的使用约定。
- 明确日志仅服务于上层项目，不复制或混入原总日志及下层项目的历史记录。

### 当前状态

- 日志文件已初始化。
- 暂无需要记录的上层项目技术决策、代码改动或未完成开发事项。

### 下一步

- 后续用户说“保存日志”时，将上层项目的最新有效上下文插入到本条记录之前。
- 新对话需要恢复上层项目状态时，优先提取本文件最上面的最新记录。

---
