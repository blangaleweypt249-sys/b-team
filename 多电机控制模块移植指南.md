# 下层多电机控制模块移植与调度指南

## 1. 文档目的

本文用于将 `user/up` 中的电机控制代码移植到下层整机，并统一 VESC、DM/J4310、RS-00
和 M2006 的任务结构。移植目标不是只做到“能够编译”，而是保持以下原则一致：

- 所有电机使用相同的任务调用结构和 1 ms 基准调度器。
- 具体发送周期由电机协议、控制要求和总线负载决定，不能全局写死为同一周期。
- 电机上电后默认禁用，不在初始化阶段自动使能。
- 默认控制目标为位置 0；只有上位机明确使能后才开始发送控制目标。
- 保留机械标零 API，但框架内部不调用。机械零点由官方上位机提前设置。
- FDCAN 中断只读取报文并写入定长队列，协议解析在任务中执行。
- 每个基准周期检查全部调度对象，但只服务本周期到期的电机或协议组。
- 不能因单游标轮询写法意外降低某台电机的目标频率。
- 不改变已经验证的协议报文、字节序、缩放公式和电机参数索引。

下层整机的目标总线分配固定为：

| 总线 | 设备 | 数量 | 目标调度 | 备注 |
| --- | --- | --- | --- | --- |
| FDCAN1 | VESC 电调电机 | 4 | 控制目标按需求周期；状态反馈必须限频 | 先确认实际 VESC 协议和反馈种类 |
| FDCAN2 | DM/J4310 | 2 | MIT 控制和必要反馈按电机要求调度 | 11 位标准经典 CAN |
| FDCAN3 | RS-00 + M2006/C610 | 2 + 2 | RS-00 按协议周期；M2006 组控制通常 2 ms | 同一总线同时规划标准帧和扩展帧过滤器 |

当前 `user/up` 代码仍是临时联调绑定：FDCAN2 连接 RS-00、FDCAN3 连接 DM。移植到下层整机
时必须交换这两条协议链路的总线绑定，并在 FDCAN3 加入 M2006 组控制。不要仅修改函数名，
必须同步修改 HAL 句柄、过滤器类型、Message RAM、任务配置和中断分发。

当前仓库尚未包含四台 VESC 和两台 M2006 的完整 Driver/App 实现。因此上表是下层目标拓扑，
不是“当前代码已经全部实现”的声明。VESC 与 M2006 接入后也必须遵循本文的统一所有权、
非阻塞发送、ISR 入队和可配置周期规则。

## 2. 当前上电行为

上电后的状态必须理解清楚，避免把“位置 0”和“机械标零”混为一谈。

1. `Up_Init()` 初始化统一 FDCAN 调度、两条总线和三类电机对象。
2. RS 和 DM 的默认位置目标均为 `0.0f deg`，上层速度统一使用 `rad/s`。
3. RS 和 DM 初始化成功后自动置为使能请求，并运动到电机已保存坐标系的 0 点。
4. M2006 初始化后保持禁用和零电流，只有调用 `Up_SetM2006Pos()` 后才使能运动。
5. 初始化和 `Up_Run1ms()` 均不会调用机械标零 API，不会重写电机零点。
6. 某台 RS/DM 反馈超时后只重新初始化一次；恢复在线后，下一次超时才允许再次触发。
7. 最后一次反馈带故障的电机不会被离线恢复逻辑自动重新使能。
8. 官方上位机负责写入并保存需要标零电机的机械零点。
9. 上层撤销使能后，周期服务负责发送停止或失能命令。

机械标零能力保留在 `RsApp_SetZero()` 和 `DmApp_SetZero()` 中，只能由
`up_main` 中明确设计的标零流程调用。目标位置为 0 只表示让电机运动到其已保存坐标系的 0，
不表示重新定义零点。

## 3. 统一分层和调用方向

两条电机链路必须遵循同一调用方向：

```text
StartLiftTask
    -> Up
        -> RS App / DM App
            -> Motor Driver / Protocol
                -> RS Bus / DM Bus
                    -> HAL FDCAN
```

发送路径：

```text
上位机命令 / 抬升动作状态机
    -> Up 更新目标和使能请求
        -> App 周期服务
            -> Driver 打包
                -> Bus / HAL FDCAN 发送
```

接收路径：

```text
FDCAN IRQ
    -> HAL_FDCAN_RxFifo0Callback
        -> Fdcan 中断分发
            -> 对应总线的软件环形队列
                -> Up_Run1ms
                    -> App / Driver 解析并更新反馈
```

禁止出现以下反向依赖：

- RS/DM App 包含 `fdcan_task.h` 或 `up_main.h` 并反向调用上层。
- Driver 直接调用上位机、机器人状态机或 FreeRTOS 任务函数。
- RS Bus 负责处理 DM Bus 的收发，或者反过来。
- ISR 直接进入 RS/DM 协议解析、模式切换或控制计算。

当前唯一 HAL FDCAN 回调位于 `Core/Src/fdcan.c` 的 CubeMX 用户区。CubeMX 重新生成后必须
确认该用户区仍然存在，并且工程中没有第二份同名 HAL 回调。

## 4. 文件职责

| 文件 | 职责 | 不应包含的内容 |
| --- | --- | --- |
| `up_main.c/.h` | 抬升电机配置、默认目标、使能策略和动作状态机 | HAL FDCAN 收发细节 |
| `fdcan_task.c/.h` | FDCAN2/FDCAN3 总线生命周期、RX 调度和中断分发 | 电机目标和动作状态机 |
| `rs_bus.c/.h` | RS 扩展帧收发、RX 环形队列 | RS 电机模式判断 |
| `dm_bus.c/.h` | DM 标准帧收发、过滤器、RX 环形队列和总线诊断 | DM MIT 控制策略 |
| `rs00.c/.h` | RS 协议、参数写入、反馈解析、非阻塞模式切换 | 多电机业务调度 |
| `rs_app.c/.h` | RS 多电机对象、目标、使能和周期服务 | HAL 中断回调 |
| `dm_motor.c/.h` | DM MIT 报文构造和反馈解析 | 固定 FDCAN 句柄 |
| `dm_app.c/.h` | DM 多电机对象、目标、使能和周期服务 | 具体 FDCAN 实例依赖 |
| `Core/Src/fdcan.c` | HAL 初始化和中断分发 | 电机协议解析 |
| `Core/Src/freertos.c` | 任务创建和 1 ms 绝对周期入口 | 电机协议细节 |

移植时应复制完整的一条纵向链路，不要只复制 `rs00.c` 或 `dm_motor.c` 后在新任务里重新拼接
一套发送、标零和使能逻辑。任务文件名可以表示物理总线，但 Driver 和 App 不能把协议永久
绑定到 FDCAN2 或 FDCAN3，否则调整接线时会再次出现跨层修改。

## 5. 移植顺序

### 5.1 先配置硬件和 CubeMX

1. 确认目标 MCU、FDCAN 实例和引脚复用。
2. 经典 CAN 设置为 1 Mbps，启用自动重传，关闭 BRS，帧最大 8 bytes。
3. 为使用的总线分配正确的标准或扩展过滤器数量。
4. 每路 RX FIFO0 建议至少 32 个元素，TX FIFO/Queue 建议至少 16 个元素。
5. 重新核算各 FDCAN 实例的 Message RAM Offset，禁止区域重叠。
6. FDCAN IRQ 优先级使用允许调用 RTOS FromISR API 的范围。当前工程为数值 5。
7. HAL 时基和 FreeRTOS Tick 必须分离。当前 HAL 使用 TIM6，RTOS Tick 为 1000 Hz。
8. 确认目标 Cortex 内核使用正确的 FreeRTOS portable 端口。

当前芯片为 Cortex-M7，但 Keil 工程路径仍指向 `portable/RVDS/ARM_CM4F`。移植前必须确认该
端口是否符合目标内核和勘误要求，不能因为工程可以链接就直接认为端口正确。若更换端口，
必须同时更换 `port.c`、`portmacro.h` 和工程 include path，不能混用两套 portable 文件。

### 5.2 加入源码和头文件

1. 把所需 `.c` 文件加入编译目标。
2. 将 `user/up` 加入 include path。
3. 确认每个 `.c` 只编译一次，尤其是 HAL 回调和 FreeRTOS port。
4. 确认只链接一个 `heap_x.c`；当前工程使用 `heap_4.c`。
5. 不要复制旧编译目录中的 `.o`、`.d`、`.crf` 代替源码。

### 5.3 接入初始化和周期任务

抬升任务只调用统一的上层入口：

```c
HAL_StatusTypeDef up_result = Up_Init();
```

周期调用保持为一个入口：

```c
if (up_result == HAL_OK)
{
    Up_Run1ms();
}
```

周期任务使用 `osDelayUntil()` 或 `vTaskDelayUntil()` 维持绝对 1 ms 周期。不要使用
`osDelay(1)`，也不要在电机 Driver 中使用 `HAL_Delay()` 补协议间隔。

RS 模式切换已经按 `now_ms` 分阶段推进。移植时不能重新改回“停止 -> Delay -> 写模式 ->
Delay -> 使能”的阻塞写法，否则一次电机切换就可能占用多个控制周期。

### 5.4 按电机特性配置调度

统一 1 ms 的是调度器基准，不是所有报文的发送周期。每类设备应提供自己的周期、相位和
到期判断，建议初始规划如下，最终数值必须根据真实协议和台架总线负载确认：

| 设备 | 控制发送建议初值 | 状态/反馈建议 | 调度单位 |
| --- | --- | --- | --- |
| 4 x VESC | 需要 1 kHz 闭环时每台 1 ms；否则按底盘控制需求降低 | 主要状态约 10 ms，温度等慢状态 50-100 ms 或关闭 | 单电机命令 |
| 2 x DM/J4310 | MIT 控制通常 1 ms | 必要反馈按电机实际上报周期接收 | 单电机命令 |
| 2 x RS-00 | 按实际控制模式和厂家协议要求配置，目标 1 ms 前必须实测 | 反馈周期与控制需求匹配 | 单电机命令 |
| 2 x M2006/C610 | 组电流控制通常 2 ms | 两台反馈通常按 2 ms 规划 | `0x200` 组命令 |

这些数值属于调度配置，不应散落在 Driver、ISR 和任务循环中重复定义。推荐每类协议维护一份
编译期调度配置，至少包含：

```text
period_ticks   周期包含几个 1 ms Tick
phase_ticks    在总线允许时错开发送相位
enabled        当前机构是否参与调度
deadline       本周期最晚发送时刻
```

每个基准 Tick 的调度逻辑应是：

```text
读取本周期完整目标快照
  -> 检查全部协议组是否到期
      -> VESC 到期则生成四台对应命令
      -> DM 到期则生成两台 MIT 命令
      -> RS-00 到期则生成两台对应模式命令
      -> M2006 到期则生成一个 0x200 组命令
  -> 按总线和截止时刻提交发送
```

这里的“检查全部协议组”不是“所有电机每 Tick 都发帧”。未到期对象只更新状态，不发送。
周期调整必须改调度配置并重新核算总线，不能在循环中增加临时 `% 2`、计数器或 `if` 特例。

FDCAN3 同时承载 RS-00 和 M2006 时，应由一个调度所有者决定两种协议的发送顺序。禁止 RS
任务和 M2006 任务分别直接争用同一个 HAL FDCAN 句柄，也不能依赖 FreeRTOS 优先级碰巧
决定谁先发送。

## 6. 多电机配置重点

### 6.1 ID 必须唯一并且成组检查

RS 配置至少需要检查：

- 每台电机 ID 在同一 RS App 中唯一。
- 主机 ID 与电机协议配置一致。
- 扩展帧过滤器能够接收该主机 ID 的反馈。
- 电机 ID 没有超过协议允许范围。

DM 配置至少需要同时检查：

- `tx_id`：控制帧发往电机的标准 ID。
- `master_id`：主控接收反馈使用的标准 ID。
- `feedback_id`：反馈数据首字节中的电机 ID。
- 同一 App 中 `tx_id` 唯一。
- 多台电机的 `master_id/feedback_id` 映射不会互相误认。

只修改头文件中的电机 ID 不够，必须同时检查任务配置表、过滤器和电机端参数。

### 6.2 DM 过滤器只适合当前两台配置

临时代码中的 DM 链路当前使用 `FDCAN_FILTER_DUAL`：

```c
filter.FilterID1 = motor_config[0].motor.master_id;
filter.FilterID2 = motor_config[1].motor.master_id;
```

这段实现明确假设只有两台 DM 电机。添加第三台及以上电机时必须重新设计过滤器，可选择：

- 为每两个标准 ID 配置一个 DUAL filter。
- ID 连续且规划明确时使用范围过滤器。
- 使用掩码过滤器，但必须证明不会接收其他协议的冲突 ID。

移植后这段过滤逻辑应跟随 DM 链路进入 FDCAN2，并检查 `hfdcan2.Init.StdFiltersNbr` 是否
足够。不能只扩大 `DM_APP_MAX_MOTORS` 后继续使用原来的两个过滤 ID，也不能继续引用
`hfdcan3`。

RS 当前用主机 ID 掩码过滤扩展帧，可以覆盖多台电机反馈，但增加电机后仍需核对协议中
电机 ID 所在位段和总线占用。

目标 FDCAN3 同时连接 RS-00 与 M2006，必须同时分配：

- RS-00 当前私有协议需要的扩展帧过滤器。
- M2006 `0x201/0x202` 反馈需要的标准帧过滤器。
- M2006 `0x200` 是主控发送的组控制 ID，不能与其他标准协议发送 ID 冲突。
- 标准帧与扩展帧的全局过滤策略都应拒绝未规划报文，不能全部放行后由业务层猜协议。

因此目标 FDCAN3 的 `StdFiltersNbr` 和 `ExtFiltersNbr` 都不能为 0。CubeMX 修改过滤器数量后
会改变 Message RAM 占用，必须重新生成并核对三路 Offset。

### 6.3 每个 1 ms 周期检查全部调度对象

调度器每 1 ms 遍历全部协议组和电机，判断它们是否到期。到期对象发送，未到期对象跳过：

```text
Tick 0: VESC due, DM due, RS due, M2006 due
Tick 1: VESC due, DM due, RS due, M2006 not due
Tick 2: VESC due, DM due, RS due, M2006 due
```

上例只是周期为 `1/1/1/2 ms` 时的示意，不是固定要求。若某类设备的实测配置为 2、5 或
10 ms，调度器应根据配置改变到期时刻，不改 Driver。

不能用一个全局游标把不同电机机械地分摊到不同 Tick：

```text
Tick 0: 只检查 motor 0
Tick 1: 只检查 motor 1
Tick 2: 只检查 motor 2
```

后一种写法会让实际周期随电机数量变化，调度配置失去意义。游标可用于已明确允许分时的
低频参数读取，但控制命令必须由各自的周期和截止时刻决定。

### 6.4 总线带宽必须先算再加电机

每增加一台电机，通常至少增加一帧周期控制和一帧周期反馈。需要考虑：

- 标准帧与扩展帧开销不同。
- 位填充会使实际帧长度波动。
- 自动重传、错误帧和仲裁等待会占用额外时间。
- 模式切换、参数写入、故障帧会形成瞬时峰值。
- TX FIFO 有空位不代表报文能在本周期内完成仲裁发送。

不要只按 `8 bytes * 帧数` 计算负载。完成静态估算后，还要在硬件上测量 bus load、最大
发送延迟、TX busy 次数、RX overflow 和 Bus-Off。

当估算负载接近 70% 时，应预留错误重传和突发命令余量。若超过目标余量，优先降低非关键
反馈频率或拆分总线，不要依赖“平均情况下能发完”。

### 6.5 队列容量与溢出

当前硬件 RX FIFO0 为 32 帧，软件 RX 环形队列也为 32 个元素。环形队列采用空槽区分满和
空，因此实际可保存 31 帧。

移植时注意：

- 队列大小必须为 2 的幂，当前通过编译期检查保证。
- ISR 写 `head`，任务写 `tail`，不能让两个上下文同时修改同一索引。
- 写完帧内容后再发布 `head`，读取完帧内容后再推进 `tail`。
- 队列满时必须有可观察的计数，不能只静默丢帧。
- 增加电机数量后要重新评估 1 ms 内最坏接收帧数。

当前 DM 协议路径有 RX overflow 诊断，RS 协议路径尚未暴露同等诊断。移植到正式整机版本
时建议统一各协议链路的诊断字段和读取接口。

## 7. 上位机命令接入

推荐的控制顺序为：

```text
上电
  -> 三路总线和电机服务初始化成功
  -> 电机保持禁用
  -> 官方上位机设置并保存机械零点
  -> 自定义上位机下发合法目标
  -> 下位机保存完整目标快照
  -> 自定义上位机请求使能
  -> 1 ms 基准任务按各电机周期发送目标
```

停止顺序为：

```text
自定义上位机撤销使能
  -> App 更新 enable_requested
  -> 下一次调度服务发送停止/失能命令
```

FDCAN1 的运动目标通常由 Chassis Task 生成，FDCAN2/3 的抬升目标通常由 Lift Task 生成。
两类业务可以分任务计算，但同一 FDCAN 句柄只能有一个发送所有者。特别是 FDCAN3 上的
RS-00 与 M2006 必须汇总到同一发送调度点。

上位机通信任务与业务任务是不同任务时，禁止一边发送控制、一边直接改写包含多个 `float`
的命令结构体。结构体复制不是天然原子的，业务任务可能读到一半旧值、一半新值。

简单可靠的方案是每台电机只保留一个完整命令邮箱或双缓冲：

1. 通信任务写入非活动缓冲区。
2. 写完全部字段后原子切换版本号或索引。
3. Lift Task 在周期开始时读取一份完整快照。
4. 只有 Lift Task 调用电机 App 和发送接口。

不要为每个字段单独加锁，也不要在 ISR 中等待互斥量。若当前阶段由同一个任务调用 Setter，
可以保持现有简单接口；一旦跨任务使用，就必须补充命令发布边界。

## 8. 跨协议行为对齐要求

| 行为 | RS | DM | 对齐要求 |
| --- | --- | --- | --- |
| 初始化结果 | `HAL_StatusTypeDef` | `HAL_StatusTypeDef` | 必须一致 |
| 默认使能 | false | false | 必须一致 |
| 默认位置目标 | 0 deg | 0 deg | 上层单位一致 |
| 上层速度单位 | rad/s | rad/s | 上层单位一致 |
| 自动机械标零 | 不调用 | 不调用 | 必须一致 |
| 控制更新 | 每 Tick 检查全部 RS 对象 | 每 Tick 检查全部 DM 对象 | 调度框架一致，周期可不同 |
| 接收处理 | ISR 入队、任务解析 | ISR 入队、任务解析 | 必须一致 |
| 模式/使能发送 | 非阻塞推进 | 单帧请求 | 均不得阻塞任务 |
| 状态查询 | has_feedback/online/active | has_feedback/online/active | 字段语义一致 |
| 故障反馈 | 撤销使能请求 | 撤销使能请求 | 基本行为一致 |

协议差异可以保留，例如 RS 使用扩展帧、DM 使用标准帧，M2006 使用组控制，VESC 状态需要
限频。Motor App 和 FDCAN Task 对业务层统一使用 `angle_deg`、`speed_rad_s`；Driver 使用协议
原生的 `position_rad`、`velocity_rad_s`。单位转换只能位于 App/Driver 边界。

任务层新增公共接口时，各协议的命名和返回语义应一起设计。例如一个模块叫
`TaskGetStatus()`，另一个模块不要再创建含义相同的 `TaskFeedback()` 和 `TaskState()` 两套
接口。

## 9. 代码风格要求

### 9.1 命名

- 类型统一使用小写加 `_t`，例如 `rs_motor_t`、`dm_app_t`。
- 布尔状态使用 `bool` 和 `true/false`，不要同一层混用 `uint8_t` 表示真假。
- 函数使用“模块 + 动作”的一致形式，例如 `RsApp_Enable()`、`DmApp_Enable()`。
- 物理量必须带单位后缀：`_rad`、`_deg`、`_rad_s`、`_rad_s2`、`_nm`、`_ms`、`_c`。
- `status` 表示函数结果，`active` 表示电机运行状态，`enable_requested` 表示上层请求，三者
  不能混用。

### 9.2 接口对称

两条线路共同需要的能力应保持相同的命名和层级：

- `TaskInit`
- `TaskRun1ms`
- `TaskSetCommand`
- `TaskEnable`
- `TaskGetStatus`
- `TaskSetMechanicalZero`
- `TaskStopAll`

协议独有能力可以单独保留，例如 RS 清故障、DM CAN 诊断，但不要为了表面对称创建没有真实
行为的空接口。

### 9.3 注释

注释应说明“为什么”，重点包括：

- 协议字段来源和字节序。
- 为什么模式切换需要时间间隔。
- 为什么某个过滤器只能支持两台电机。
- 为什么标零 API 不在初始化阶段调用。
- 总线频率和控制周期的依据。

不要给显而易见的赋值、循环和空指针检查添加逐行说明。

## 10. 避免重复和过度设计

移植时不要重新引入已经删除的问题：

- 不要为每一种 RS 模式在 Task、App、Driver 各写一套一比一 Setter。
- 不要同时提供宏构造、通用命令、类型化命令三套等价入口。
- 不要在 Task 层重复检查一遍 App 已经能够判断的电机 ID。
- 不要用多个布尔标志分别描述同一个状态机阶段。
- 不要为一行 `time_reached()` 或 `clamp()` 建立跨模块公共框架。
- 不要为了“未来可能有用”提前暴露没有调用者的 API。
- 不要建立全局 bus/motor 注册表后，又在 App 中保存同一批对象。

需要抽象的地方是所有权和边界，而不是把每个简单表达式都包装成公共函数。

## 11. 常见移植错误

### 11.1 只增加配置数组，不修改过滤器

结果：第三台 DM 电机能发送控制，但反馈永远进不了 RX FIFO。

### 11.2 使用游标每毫秒只服务一台电机

结果：电机越多，单电机控制频率越低，表面任务仍是 1 kHz，实际控制不是 1 kHz。

### 11.3 在 ISR 中直接调用 `RsMotor_Parse()`

结果：各协议并发模型不一致，中断执行时间随 FIFO 帧数和解析复杂度增长。

### 11.4 在 Driver 中使用 `HAL_Delay()`

结果：1 ms Lift Task 被模式切换占用数毫秒，`osDelayUntil()` 连续超期。

### 11.5 初始化时自动使能或自动标零

结果：上电行为依赖代码默认值，可能覆盖官方工具保存的零点，或在上位机未接管前运动。

### 11.6 混用角度单位

结果：上层的 90 deg 被 Driver 当作 90 rad，或 `rad/s` 被误当作 `deg/s` 再次换算。上层只使用
`angle_deg` 和 `speed_rad_s`，角度转换集中在 App/Driver 边界，不能依靠调用者记忆。

### 11.7 只看编译成功

结果：FreeRTOS portable、IRQ 优先级、Message RAM、过滤器或物理接线错误直到上电才暴露。

## 12. 编译前检查清单

- [ ] 目标芯片、Cortex 内核和 FreeRTOS portable 匹配。
- [ ] 只链接一套 FreeRTOS port 和一个 heap 实现。
- [ ] FDCAN 为经典 CAN，1 Mbps，BRS 关闭。
- [ ] FDCAN1 的 VESC 扩展帧过滤器和状态 ID 规划正确。
- [ ] FDCAN2 的 DM 标准帧过滤器数量足够。
- [ ] FDCAN3 同时分配了 RS-00 扩展过滤器和 M2006 标准过滤器。
- [ ] 三路 Message RAM 区域没有重叠。
- [ ] RX FIFO 和 TX Queue 容量满足最大电机数量。
- [ ] FDCAN IRQ 优先级符合 FreeRTOS 约束。
- [ ] 全工程只有一个 `HAL_FDCAN_RxFifo0Callback()`。
- [ ] 所有电机 ID、主机 ID 和反馈 ID 唯一且与电机端一致。
- [ ] RS 与 DM 默认目标均为物理位置 0。
- [ ] 初始化阶段没有自动使能调用。
- [ ] 初始化和周期任务没有机械标零调用。
- [ ] `TaskRun1ms()` 内没有 `HAL_Delay()` 或等待 TX FIFO 的循环。
- [ ] 每个 1 ms 基准 Tick 检查全部调度对象，并只发送本 Tick 到期的对象。
- [ ] VESC、DM、RS-00、M2006 周期来自集中配置，没有散落的临时分频判断。

## 13. 台架验收清单

建议先单电机、再同总线多协议、最后三路同时运行。

### 13.1 单电机

- [ ] 上电后电机不使能、不主动运动。
- [ ] 官方上位机设置零点后重新上电，零点保持。
- [ ] 自定义上位机下发目标但未使能时，电机不运动。
- [ ] 使能后第一控制目标为位置 0。
- [ ] 撤销使能后收到停止或失能命令。
- [ ] 反馈位置、速度、力矩、温度和故障字段合理。

### 13.2 同总线多电机

- [ ] 每台电机实际控制周期等于其调度配置，不随同总线电机数量变化。
- [ ] 每台反馈只更新对应电机对象，不发生 ID 串扰。
- [ ] 同时模式切换时任务没有数毫秒阻塞。
- [ ] TX busy 和 RX overflow 在可接受范围内。
- [ ] 总线负载和最大仲裁延迟满足控制要求。
- [ ] 断开任意一台电机不会影响其他电机 ID 解析。

### 13.3 三路同时运行

- [ ] FDCAN1、FDCAN2、FDCAN3 的 HAL 回调分发正确。
- [ ] FDCAN1 四台 VESC 控制时，状态反馈限频有效。
- [ ] FDCAN2 两台 DM 的控制和反馈周期符合配置。
- [ ] FDCAN3 RS-00 与 M2006 同时运行时，标准帧和扩展帧都能正确接收。
- [ ] M2006 两台电机共用一个组控制帧，没有重复发送两份 `0x200`。
- [ ] 一路高负载不会让另一路停止执行周期服务。
- [ ] Lift Task 没有持续超期。
- [ ] 各路状态查询字段含义一致。
- [ ] 上位机能够分别使能、停用和更新每台电机。

## 14. 移植完成标准

同时满足以下条件才算完成移植：

1. 全量重新构建为 0 error、0 warning。
2. `.map` 文件确认 FreeRTOS port、heap 和电机源码各只有一份。
3. 上电默认禁用，框架没有自动机械标零调用。
4. VESC、DM、RS-00、M2006 的任务调用和状态接口遵守同一风格。
5. 每个调度对象按自己的配置周期执行，不因电机数量或游标轮询降频。
6. ISR 只收帧入队，协议解析全部在任务中完成。
7. 多电机同时工作时完成总线负载、延迟、丢帧和故障测试。
8. 自定义上位机与 Lift Task 之间使用完整命令快照，不读取半更新结构体。

任何一项只在代码层“看起来正确”但没有通过台架验证，都应记录为待验证，而不能写成已经
稳定运行。
