# H723VGT6-下层代码框架规范

## 1. 当前范围

`H723VGT6-下层` 当前只有框架设计，尚未创建工程目录。本文件规定下层的代码分层、三路
FDCAN、FreeRTOS 任务和内核边界，不代表底盘或抬升功能已经实现。本轮不创建任务文件、
不填写任务函数，也不加入 UI、Tool、Python、Log 或 Auto Test 调试层代码。

代码调用方向固定为：

```text
Application（Robot / Task / State Machine）
    -> App（底盘 / 抬升）
        -> Algorithm / Driver
            -> BSP / HAL
```

Application、App 和 Algorithm 不得直接调用 HAL；算法层不得发送 CAN，业务层不得拼接
电机协议报文或直接操作寄存器。

## 2. 目标目录和职责

```text
H723VGT6-下层/
|-- Core/                         CubeMX 生成入口和 HAL 外设初始化
|-- BSP/                          硬件层
|   |-- Can/                      三路 FDCAN 的 HAL 收发封装
|   `-- Communication/            通信中断到任务的通知入口
|-- Driver/                       驱动层
|   |-- Can/                      与 HAL 无关的 CAN 帧类型
|   `-- Motor/                    U12、RS-00、J4310、M2006 型号适配
|-- Algorithm/                    算法层
|   |-- Chassis/                  底盘运动学
|   `-- Pid/                      纯控制计算，不访问硬件
|-- App/                          业务层
|   |-- Chassis/                  4 x U12 底盘
|   `-- Lift/                     2 x RS-00 + 2 x J4310 + 2 x M2006 抬升
|-- Application/                  应用层
|   |-- Communication/            上下层通信、命令和超时处理
|   |-- Robot/                    下层整机状态和安全编排
|   `-- Task/                     FreeRTOS 任务入口和任务间通信
|-- Config/                       参数、接线方案和 ID 集中配置
|   |-- can_id.h
|   |-- lower_config.h
|   `-- lower_config.c
|-- Drivers/                      ST 官方 HAL/CMSIS，不是自写 Driver 层
|-- Middlewares/                  FreeRTOS
`-- MDK-ARM/                      Keil 工程
```

`Driver/` 是队内设备驱动，`Drivers/` 是 STM32 固件包，两者不能混放。所有周期、限幅、
超时、节点 ID、接线方案和机构参数集中到 `Config/`，业务函数中禁止散落魔法数字。

## 3. 代码边界

- 普通变量使用小写加下划线；物理量保留 `_rad`、`_rad_s`、`_a`、`_ms` 等单位后缀。
- 宏全部大写，枚举和结构体类型使用 `_t` 后缀，函数使用 `模块_动作()` 形式。
- 每个功能使用独立 `.c/.h`；能设为 `static` 的数据和函数不暴露为全局符号。
- `.c` 文件顺序固定为 include、宏、私有类型、static 数据、全局数据、函数。
- CAN ID 只在 `Config/can_id.h` 或协议驱动中定义；App 不出现协议仲裁 ID。
- 控制顺序固定为“读取反馈 -> 状态更新 -> PID/算法 -> 业务目标 -> CAN 调度发送”。
- ISR 只取数据和通知任务；任务中禁止使用阻塞式 UART/CAN 发送。
- 对外接口检查空指针、长度、状态和范围；通信超时后先停输出，不复用过期目标。
- CubeMX 再生成只能覆盖生成区，自写代码放在 `BSP/Driver/Algorithm/App/Application`。

完整的通用格式要求以仓库根目录 `控制组代码规范.md` 为准。

## 4. 三路 CAN、机构分配和余量

下层内部控制和任务调度按 1 kHz 设计，三路 FDCAN 均配置为 1 Mbps 经典 CAN。
`FDCAN_FRAME_CLASSIC` 必须启用，BRS 必须关闭，单帧最大 8 bytes。FDCAN 是 H723 的外设
名称，不代表电机必须使用 CAN FD；不得向只支持经典 CAN 的节点发送 CAN FD 报文。

FDCAN1 固定连接 `4 x U12/VESC`，控制 1 kHz，主要状态先设为 100 Hz。抬升电机允许采用
下面两种接线配置，配置由固件和线束明确选择，不能在运行期间自动猜测：

| 配置 | FDCAN2 | FDCAN3 |
| --- | --- | --- |
| A：M2006 与 J4310 共线 | 2 x RS-00，完整控制和反馈 1 kHz | 2 x J4310，完整控制和反馈 1 kHz；2 x M2006/C610，组控制和反馈 500 Hz |
| B：M2006 与 RS-00 共线 | 2 x RS-00，必须切换为 11 位标准帧 MIT，完整控制和反馈 1 kHz；2 x M2006/C610，组控制和反馈 500 Hz | 2 x J4310，完整控制和反馈 1 kHz |

两种配置都允许 RS-00、J4310 和 M2006 同时工作。RS-00 和 J4310 的目标每 1 ms 更新一次，
每台电机也按 1 kHz 返回反馈；两个 M2006 共用一个组控制帧，每 2 ms 更新一次，两台 C610
各按 500 Hz 反馈。J4310 即使只保持固定角度，也继续按完整 1 kHz 通信计算占用。

| 下层总线/工况 | 周期报文与估算占用 | 结论 |
| --- | --- | --- |
| FDCAN1，4 x U12/VESC | 4 个扩展控制帧/ms，加 100 Hz 主要状态约 58%-70% | 可实现 1 kHz 指令，反馈必须限频 |
| 配置 A，RS-00 独占 FDCAN2 | 2 控制 + 2 反馈扩展帧/ms，约 52%-64% | 可完整运行 1 kHz |
| 配置 A，J4310 + M2006 | J4310 为 4 个标准帧/ms；M2006 平均为 0.5 个组控制帧/ms + 1 个反馈帧/ms；合计 5.5 帧/ms，约 61%-74% | RS-00 和 J4310 保持 1 kHz，M2006 保持 500 Hz，可实施 |
| 配置 B，RS-00 MIT + M2006 | RS-00 为 4 个标准帧/ms；M2006 平均为 1.5 个标准帧/ms；合计 5.5 帧/ms，约 61%-74% | RS-00 和 J4310 保持 1 kHz，M2006 保持 500 Hz，可实施 |
| 配置 B，J4310 独占 FDCAN3 | 2 控制 + 2 反馈标准帧/ms，约 44%-54% | 可完整运行 1 kHz |

U12 按当前本地 VESC 驱动计算：控制命令和 STATUS 使用 29 位扩展经典 CAN。四台 U12
不能都以 1 kHz 上报完整状态；一个必要 STATUS 报文先设为 100 Hz，温度和累计量等状态
设为 10-20 Hz 或关闭。如果 U12 实际不使用 VESC，必须取得真实协议后重新核算。底盘轮型、
轮半径、轮距和轴距也必须确认后才能实现底盘解算。

C610 即使 M2006 不转也会周期反馈。两个 C610 的组控制和反馈统一设为 500 Hz；M2006
与哪一组共线，就必须在该总线上为所有标准帧规划不冲突的 ID。C610 使用 `0x200` 组控制
和 `0x201/0x202` 反馈；J4310 速度模式使用 `0x200 + ID`，RS-00 MIT 也在 11 位 ID 中编码
模式和电机 ID，均不得与这些 ID 重号。过滤器必须按当前配置明确区分协议。

配置 B 实施前，必须使用厂家协议切换命令或官方工具把两台 RS-00 从默认私有协议切换为
标准帧 MIT，并在重新上电后确认设置保持。现有基于 29 位扩展私有协议的驱动不能直接复用，
必须重新验收 RS-00 MIT 控制、反馈、使能、停止、故障和超时处理。

工程建立时每路至少分配 8 个 RX FIFO 和 TX Queue 元素，并重新规划 FDCAN Message RAM、
过滤器、中断分发和周期发送调度。

## 5. FreeRTOS 任务框架

下层规划三个业务任务，具体任务函数本轮保持空缺：

| 任务 | 负责机构 | 触发方式 | 职责边界 |
| --- | --- | --- | --- |
| `lowerSafetyTask` | 下层全部输出 | 事件唤醒，并以 1 ms 超时周期复查 | 最高业务优先级；汇总急停、上下层通信超时、电机离线、限位和任务超期，发布全局输出禁止状态 |
| `lowerChassisTask` | 4 x U12 | 固定 1 ms 周期 | 读取运动目标，完成底盘解算、约束和四轮目标计算 |
| `lowerLiftTask` | 2 x RS-00 + 2 x J4310 + 2 x M2006 | 固定 1 ms 周期 | 完成抬升状态机、同步约束和控制计算；三组电机可并行产生目标 |

`commRxTask`、`motorTxTask` 和 `monitorTask` 是基础设施任务，分别作为接收解析唯一入口、
周期发送唯一出口和低优先级运行监控，不承载底盘或抬升业务。相对优先级固定为
`lowerSafetyTask > commRxTask > motorTxTask > 下层业务任务 > monitorTask`；底盘任务与
抬升任务使用同一优先级。

`lowerLiftTask` 每个 1 ms 周期生成同一份完整抬升目标快照：RS-00 和 J4310 每周期更新，
M2006 每两个周期更新。`motorTxTask` 根据编译期接线配置把三组报文路由到 FDCAN2/FDCAN3，
并按周期号统一发送；禁止两个任务分别控制同一抬升状态，也不能依靠任务优先级决定发送
先后。任一组故障时，安全状态机必须能独立禁止该组输出，并根据机械耦合关系决定是否同时
停止另一组。

业务任务只提交带周期号的目标快照和周期完成位。`motorTxTask` 等待全部完成位或本周期
截止时刻；任务缺席或目标过期时，对对应机构使用安全目标并记录超期，不能复用旧目标。

## 6. FreeRTOS 内核规范

### 6.1 内核版本、异常和 Tick

- 以上层已验证的 FreeRTOS V10.6.2、CMSIS-RTOS2、Cortex-M7 端口和 `heap_4` 作为基线。
  不得混用其他版本的头文件、`cmsis_os2.c`、portable 端口或 heap 文件；工程只能链接一套
  内核源码和一个 heap 实现。
- 使用抢占式调度，`configTICK_RATE_HZ` 为 1000。FreeRTOS 使用 SysTick，HAL 时基使用
  TIM6；不得让 HAL 和内核同时接管 SysTick。
- `SVC_Handler`、`PendSV_Handler` 和 `SysTick_Handler` 只能由 Cortex-M7 FreeRTOS 端口
  接管一次。CubeMX 重新生成后必须检查向量表和中断文件没有重复定义；PendSV 和 SysTick
  保持最低可抢占优先级。
- Tick 到期只代表任务变为 Ready。1 ms 周期任务必须使用绝对周期 `osDelayUntil()`，保存
  并递增 `next_tick`，同时统计迟到和超期；不能用 `osDelay(1)` 或非阻塞轮询维持周期。

### 6.2 中断优先级和 ISR

- 使用 `configLIBRARY_MAX_SYSCALL_INTERRUPT_PRIORITY = 5`。NVIC 数字越小，实际优先级
  越高：数值 0-4 的 IRQ 禁止调用 FreeRTOS/CMSIS-RTOS2 接口；需要通知任务的 FDCAN、
  UART、DMA 中断必须设置为数值 5-15，不能依赖复位默认值 0。
- ISR 只清标志、搬取必要的固定长度数据并通知任务，不能循环解析完整协议、做控制计算、
  格式化输出、动态分配内存、等待互斥量或调用阻塞接口。
- 原生 FreeRTOS API 必须使用 `FromISR` 版本，队列等待时间必须为 0，并按 API 返回值请求
  中断退出后的调度。数值 0-4 的 IRQ 也不能通过 HAL 回调间接进入 RTOS。

### 6.3 通信、共享资源和安全状态

- CAN 帧、机构命令和反馈快照使用定长队列或双缓冲按值传递。线程标志/任务通知只表达事件
  或周期完成位，不能用一个 bit 统计多次事件。安全事件必须锁存，不能因队列满而静默丢失。
- 共享外设采用“单一任务拥有 + 其他任务发消息”。互斥量不能在 ISR 使用；持锁期间禁止
  延时、打印或等待另一把锁，并统一多锁获取顺序。
- `volatile` 不能替代队列、临界区、原子操作或 Cache 一致性处理。业务任务只读取完整发布
  的快照，不能直接读写另一任务的可变私有数据。
- 安全状态是所有电机目标的最终门控条件。看门狗只能在安全任务确认必要任务都更新心跳、
  无关键超期后喂狗，不能由某个始终运行的任务单独喂狗。

### 6.4 栈、堆和故障钩子

- 任务栈、FreeRTOS 堆、C 库堆和 MSP 是四类独立内存。CMSIS-RTOS2 的 `stack_size` 单位
  是 bytes，原生 `xTaskCreate()` 的栈深单位是 `StackType_t` 数量，不能直接照抄数值。
- `heap_4` 和 64 KiB `configTOTAL_HEAP_SIZE` 只作为初始基线。RTOS 对象集中在启动阶段
  创建并检查句柄；运行阶段禁止反复创建对象。
- 必须启用 `configCHECK_FOR_STACK_OVERFLOW = 2`、malloc 失败钩子和 `configASSERT`。
  `monitorTask` 至少记录任务最小栈余量、RTOS 堆余量、MSP 余量、malloc 失败、栈溢出
  任务名、HardFault 寄存器、周期超期、队列溢出和 CAN 错误。

### 6.5 STM32H7 D-Cache 与 DMA

- DMA 缓冲区必须先确认位于对应 DMA 可访问的 RAM，再处理 Cache 一致性，并通过 `.map`
  文件核对最终地址。
- H723 的 D-Cache line 为 32 bytes。DMA 缓冲区按 32 bytes 对齐并尽量独占 Cache line；
  DMA 发送前 Clean，DMA 接收启动前按统一封装 Clean/Invalidate，完成后 CPU 读取前再次
  Invalidate。DMA 完成前 CPU 不得改写仍归 DMA 所有的缓冲区。
- 也可以用 MPU 建立 non-cacheable DMA 区域，但整个工程只能选择一种统一封装策略，不能
  让每个驱动自行维护 Cache。

## 7. 物理层和验收要求

- 每条 CAN 使用线型主干或菊花链，支线尽量短；每条独立总线只在两端各放一个 120 欧终端，
  断电测 CAN_H 与 CAN_L 应约为 60 欧。
- 每路 CAN 必须有独立收发器，主控、电机和电调必须共参考地。
- 每路 RX FIFO 至少保留 8 个元素；过滤器按总线和标准/扩展帧规划，不能长期全部放行后靠
  业务层猜协议。
- 联调时记录实际 bus load、最大报文延迟、发送队列峰值、RX FIFO 丢帧、TEC/REC、Bus-Off
  和控制周期超时。
- FDCAN1 必须确认 U12 使用当前 VESC 协议并核对 STATUS 帧数。配置 B 必须确认 RS-00 已
  进入标准帧 MIT，并在 `RS-00 1 kHz + M2006 500 Hz` 同时工作时实测总线负载和最大延迟。
  未满足这些条件只能称为“目标频率”，不能写成“已经验证稳定运行”。

## 8. 实施原则

1. 底盘和抬升任务只提交物理目标，电机协议保留在 Driver。
2. 接收解析和周期发送均由单一基础设施任务负责，业务任务不争用 HAL 句柄。
3. 下层工程从创建时就落实第 6 节约束，不能先用全局变量和 ISR 解码临时跑通，再把竞态、
   内核优先级和 Cache 问题留到联调阶段处理。
4. U12 或 RS-00 的实际协议与当前假设不一致时，先重新核算总线负载和过滤器，再修改实现。
