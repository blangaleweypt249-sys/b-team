# 控制组代码规范 V1.1

本规范专门针对 **Robocon 控制组（STM32H723 + HAL + FDCAN + FreeRTOS）** 制定，不追求复杂的软件工程规则，核心目标只有三个：

1. 比赛期间每个人都能看懂。
2. 代码出问题时能快速定位。
3. 方便多人协作，也方便 AI 辅助开发与维护。

## 当前工程修改边界

**日常开发、功能新增和控制流程修改只进入 `User/Application/`。** 该目录是当前工程的
应用层，也是队员日常修改的唯一入口，只包含任务入口、动作状态机、模式切换和控制流程。

**除 `User/Application/` 外，其余目录默认都是底层/基础设施或配套基线，日常不用修改。**
包括 `User/App/`、`User/Algorithm/`、`User/Driver/`、`User/BSP/`、`User/Tests/`、
`Core/`、`Drivers/`、`Middlewares/`、`MDK-ARM/`。只有明确分配了底层维护、
版本升级或缺陷修复任务，并完成评审和验证后，才允许修改这些目录；普通需求不得通过改底层
代码来实现。

---

## 一、命名规范

### 1. 变量

统一采用 **小写 + 下划线**：

```c
float target_speed;
float current_speed;
uint8_t motor_state;
```

不要这样写：

```c
float TargetSpeed;
float a;
float temp1;
```

### 2. 宏定义

全部大写：

```c
#define MAX_SPEED      5.0f
#define WHEEL_RADIUS   0.08f
#define PI             3.1415926f
```

### 3. 枚举

枚举类型：

```c
typedef enum
{
    MOTOR_STOP,
    MOTOR_RUN,
    MOTOR_ERROR
} motor_state_t;
```

变量：

```c
motor_state_t motor_state;
```

### 4. 结构体

```c
typedef struct
{
    float speed;
    float angle;
    float current;
} motor_data_t;
```

统一加 `_t` 后缀。

### 5. 函数命名

统一采用：

`模块_动作()`

例如：

```c
Motor_Init();
Motor_SetSpeed();
Motor_Stop();
Can_Send();
Can_Receive();
Imu_Update();
Vision_Parse();
```

不要这样写：

```c
Init();
Run();
test();
abc();
```

### 6. 命名长度

变量名和函数名在含义清楚的前提下应尽量简短，建议不超过 **20 个字符或 4 个英文单词**；协议名、硬件型号等固定名称可以例外。

可以统一使用团队内含义明确的常用缩写：

```text
velocity -> vel
position -> pos
command  -> cmd
config   -> cfg
current  -> cur
previous -> prev
maximum  -> max
minimum  -> min
```

单位后缀不能为了缩短命名而省略，例如 `_m_s`、`_rad_s`、`_ms` 必须保留。不要自造生僻缩写，也不要把变量缩成无法理解的单字母。

推荐：

```c
float target_vel_m_s;
float cur_yaw_rad;
void Chassis_GetKeyVel(void);
```

不推荐：

```c
float target_linear_velocity_meter_per_second;
void Chassis_CalculateKeyboardVelocityCommand(void);
```

---

## 二、文件组织

按模块拆分文件，例如：

电机：

```text
Motor.c
Motor.h
```

IMU：

```text
Imu.c
Imu.h
```

CAN：

```text
Can.c
Can.h
```

底盘：

```text
Chassis.c
Chassis.h
```

视觉：

```text
Vision.c
Vision.h
```

每个模块只负责自己的事情。

例如：

- `Motor.c` 不要去解析视觉。
- `Vision.c` 不要去控制 PWM。

---

## 三、每个 `.c` 文件格式

统一按如下顺序组织：

```c
#include "Motor.h"

#define ...

typedef ...

static ...

全局变量

函数
```

例如：

```c
#include "Motor.h"

#define CURRENT_LIMIT 10

static float motor_offset;

motor_data_t motor_data;

void Motor_Init(void)
{

}

void Motor_Update(void)
{

}
```

---

## 四、变量作用域

原则：

> 能 `static` 绝不全局

例如：

```c
static float imu_bias;
```

只有多个文件都需要的数据，才放到 `extern`：

```c
extern robot_state_t robot;
```

不要几十个全局变量到处飞。

---

## 五、注释规范

不要写：

```c
//速度
```

要写：

```c
// 根据目标速度计算电流输出
```

函数统一写标准注释：

```c
/**
 * @brief 电机速度控制
 * @param target_speed 目标速度(rad/s)
 * @retval None
 */
```

---

## 六、`if` 格式格式

统一写法：

```c
if (speed > MAX_SPEED)
{
    ...
}
else
{
    ...
}
```

不要这样写：

```c
if(speed>MAX_SPEED){
...
}
```

---

## 七、`switch` 格式规范

```c
switch (state)
{
case MOTOR_STOP:
    ...
    break;

case MOTOR_RUN:
    ...
    break;

default:
    break;
}
```

---

## 八、魔法数字

禁止直接写：

```c
speed *= 57.3;
if (error > 0.15)
```

统一提取定义：

```c
#define RAD_TO_DEG     57.2958f
#define YAW_THRESHOLD  0.15f
```
但是延时可以直接写数字，
以后改参数只改一个地方。

---

## 九、PID 命名规范

统一命名：

```c
pid_t speed_pid;
pid_t yaw_pid;
pid_t angle_pid;
```

统一调用形式：

```c
PID_Calc(&speed_pid,
         target_speed,
         current_speed);
```

不要这样写：

```c
Speed_PID();
PID2();
PID_new();
```

---

## 十、CAN 规范

板级逻辑总线、节点和过滤 ID 统一放在 `User/BSP/Can/bsp_can.h`；具体电机协议 ID 由
`User/Driver/` 中的型号驱动管理。应用层和算法层不得直接写协议数字。

```text
User/BSP/Can/bsp_can.h
```

当前拓扑示例：

```c
#define CAN_BUS_ARM_J4310      1U
#define CAN_BUS_ARM_M3508      2U
#define CAN_BUS_AUX            3U
#define NODE_ARM_J4310         0x06U
#define NODE_GATE_M2006        1U
#define NODE_GRIPPER_M2006     2U
```

不要到处散落：

```text
0x201
0x321
0x835
```

以后改 ID 很方便。

---

## 十一、状态机

禁止大量分散的条件判断：

```c
if (...)
{
    ...
}

if (...)
{
    ...
}

if (...)
{
    ...
}
```

统一写成状态机：

```c
typedef enum
{
    ROBOT_INIT,
    ROBOT_READY,
    ROBOT_RUN,
    ROBOT_ERROR
} robot_state_t;
```

然后：

```c
switch (robot.state)
{
    ...
}
```

可维护性高很多。

---

## 十二、控制周期

例如 `1kHz` 控制周期，统一执行顺序：

```text
读取传感器
-> 状态更新
-> PID，运动学
-> CAN发送
```

执行顺序固定，不允许有人随便改。

---

## 十三、日志输出

禁止直接到处写：

```c
printf("%f", speed);
```

统一使用日志接口：

```c
LOG_INFO("speed=%.2f", speed);
LOG_ERROR("CAN timeout");
LOG_WARN("IMU drift");
```

后期关闭日志只改一个宏。

---

## 十四、目录规范

以下是当前 `上层分支/H723VGT6-上层` 的实际目录。方括号中的标记是修改权限，不是编译
层级：

```text
H723VGT6-上层/
├── Core/                         [底层，不改] CubeMX 生成的外设初始化、IRQ 和 HAL 桥接
│   ├── Inc/                      生成的头文件
│   ├── Src/                      生成的源文件
│   ├── Inc/Backup/               Inc 头文件历史备份，只读留档
│   └── Src/Backup/               Src 源文件历史备份，只读留档
├── Drivers/                      [底层，不改] ST 官方 CMSIS 和 STM32H7xx HAL/LL
│   ├── CMSIS/                    Cortex、芯片头文件及官方 DSP 组件
│   └── STM32H7xx_HAL_Driver/     STM32H7 HAL/LL 外设实现
├── Middlewares/                  [底层，不改] 第三方中间件
│   └── Third_Party/FreeRTOS/     FreeRTOS 内核、CMSIS-RTOS2 适配和 heap
├── MDK-ARM/                      [工程基线，不改业务] Keil 工程、RTE、编译产物和烧录记录
├── User/                         队内代码总目录；日常只进入 Application
│   ├── App/                      [底层基线，不改] 稳定机构与整机业务模块
│   │   ├── Arm/                  J4310 + 2 x M3508 机械臂、轨迹和自动回零
│   │   ├── Common/               各机构共用的稳定数据结构
│   │   ├── Gate/                 M2006 开关门
│   │   ├── Gripper/              M2006 夹爪
│   │   └── Robot/                整机对象、基础状态和机构编排
│   ├── Algorithm/                [底层基线，不改] 纯算法
│   │   ├── Pid/                  PID 计算
│   │   └── MotorOnlineTune/      电机在线整定辅助算法
│   ├── Driver/                   [底层基线，不改] 设备协议和设备驱动
│   │   ├── Flash/                W25Qxx 外部 Flash 驱动
│   │   ├── Motor/                电机管理、统一端口和型号协议
│   │   │   ├── DJI/              DJI 组帧公共封装
│   │   │   ├── DM J4310/         达妙 J4310 MIT 协议
│   │   │   ├── M2006/            M2006 + C610 协议
│   │   │   └── M3508/            M3508 + C620 协议
│   │   ├── PcLink/               小电脑帧、会话、命令 DTO 和状态编解码
│   │   ├── RemoteLink/           遥控定长帧解析、重同步和链路状态
│   │   └── Vofa/                 VOFA 电机调试桥接，不加入生产固件
│   ├── BSP/                      [底层基线，不改] 板级硬件抽象
│   │   ├── Can/                  三路 FDCAN、帧类型、过滤器和 HAL 发送封装
│   │   └── Communication/        UART/DMA、FDCAN 接收运行时封装
│   ├── Tests/                    [测试基线，不改] 不加入固件的主机侧回归测试
│   └── Application/              [日常修改] 应用层
│       ├── FreeRTOSTasks/        RTOS 任务创建和任务函数桥接
│       ├── my_main/              应用启动入口和初始化编排
│       └── Task/                 周期任务、动作状态机、通信事件映射和遥控流程
├── 上位机/                       [配套工具，不改板端业务] Windows 上位机、协议和参数工具
├── H723VGT6.ioc                  [底层配置，不改] CubeMX 工程配置
└── cubemx_* / mx.*               [底层工具，不改] CubeMX 生成和后处理脚本/临时文件
```

文件夹含义可以按下面的原则快速判断：`Core/` 是芯片工程入口，`Drivers/` 是 ST 官方库，
`Middlewares/` 是 RTOS 等第三方库，`MDK-ARM/` 是编译工程；它们都不是日常业务代码。
`User/BSP/` 把板级硬件差异封装起来，`User/Driver/` 处理具体设备协议，`User/Algorithm/`
只做计算，`User/App/` 保存稳定的机构和整机业务基线，`User/Application/` 只编排需要日常修改的动作和流程。
`User/Tests/` 只在主机侧验证，`上位机/` 只提供调试和参数工具，两者都不进入固件控制路径。

STM32/CubeMX 工程必须遵守以下落盘边界：

1. 日常新增或修改的 `.c/.h`、任务、动作状态机以及通信事件到业务动作的映射统一放在
   `User/Application/`；帧解析、CRC、握手、会话、链路状态和协议 DTO 留在 `User/Driver/`。
   不得把普通需求直接写入 `Core/`、`Drivers/`、`Middlewares/` 或其他底层目录。
2. `Core/` 只保留 CubeMX 生成文件。确需连接 `User/Application/` 时，只在
   `USER CODE BEGIN/END` 区写 include、初始化或任务入口等最小桥接。
3. `Drivers/` 和 `Middlewares/` 视为厂商或第三方代码；队内 `User/Driver/` 不得与厂商
   `Drivers/` 混用。`MDK-ARM/`、`H723VGT6.ioc` 和 `cubemx_*` 也属于底层基线。
4. 移动或新增文件时必须同步更新 Keil/CMake 等工程源文件路径和 include 路径；CubeMX 再生成
   后必须检查工程配置没有退回旧路径。

这样既能一眼定位代码归属，也能避免 CubeMX 再生成覆盖自写框架。

---

## 十五、代码分层

为了避免功能越写越乱，当前上层工程统一按“自底向上”分层，不允许跨层乱调用。这里的
“层”表示代码职责，不等同于文件夹名称；实际文件夹对应关系如下：

```text
应用层        User/Application/       [日常修改]
业务层        User/App/                [底层基线]
算法层        User/Algorithm/          [底层基线]
驱动层        User/Driver/             [底层基线]
硬件抽象层    User/BSP/                [底层基线]
芯片底层      Core/ Drivers/           [底层基线]
系统中间件    Middlewares/             [底层基线]
测试/调试配套  User/Tests/ 上位机/       [配套基线]
```

各层职责如下：

### 1. 应用层：`User/Application/`（日常修改入口）

应用层负责“整机现在要做什么”，也是本工程唯一的日常修改目录。它可以调用已经存在的
业务和驱动接口，但不应把稳定整机封装、底层协议或寄存器操作重新写进来。

当前子目录含义：

- `FreeRTOSTasks/`：创建和连接 RTOS 任务。
- `my_main/`：应用启动和初始化编排。
- `Task/`：周期任务入口、动作状态机、通信事件到机器人目标的映射和遥控流程。

新增一个日常功能时，优先在这里新增或修改文件；需要改变底层接口时必须单独提出底层维护
任务，不能顺手修改其他层。

### 2. 业务层：`User/App/`

业务层保存不需要日常修改的稳定控制能力。当前包括 `Arm/`（J4310 + 2 x M3508 机械臂、
位置轨迹和掉线重连自动回零）、`Gate/`（M2006 开关门）、`Gripper/`（M2006 夹爪）以及
`Robot/`（整机对象、基础运行状态和机构编排）。它可以调用算法层和驱动层，但不直接碰寄存器。

该目录是已整理好的底层业务基线，普通需求通过应用层调用其现有接口；日常不要直接修改。

### 3. 算法层：`User/Algorithm/`

算法层只负责可复用的计算，不直接操作硬件，不发送 CAN，也不依赖具体机构。当前
`Pid/` 负责 PID 计算，`MotorOnlineTune/` 负责电机在线整定辅助算法。

算法层输入反馈和目标，输出计算结果；参数、单位和限幅必须由调用方按接口明确传入。

### 4. 驱动层：`User/Driver/`

驱动层负责具体设备和外部链路的数据收发、协议编解码及链路状态。当前 `Flash/` 是 W25Qxx
外部 Flash 驱动，`Motor/` 是电机管理、统一端口和 J4310/M2006/M3508/DJI 型号协议，
`PcLink/` 处理小电脑二进制帧、CRC16、会话和协议 DTO，`RemoteLink/` 解析遥控链路。

驱动层只提供稳定接口，不引用 `User/App/` 或 `User/Application/` 的业务类型，也不决定正式
控制流程中的机械臂、开关门或夹爪目标。具体协议 ID、组帧和 HAL 调用必须留在这一层或 BSP 层。
`Vofa/` 是例外的非生产调试桥接：它接收文本命令，直接测试 M2006/M3508 的电流、速度、位置、
PID 和自动整定并回传遥测；它由工程后处理脚本排除，不进入比赛固件控制路径。

### 5. 硬件抽象层：`User/BSP/`

板级支持包（BSP）把本块 H723 板子的硬件差异封装起来。`Can/` 负责三路 FDCAN、帧类型、
过滤器和 HAL 发送封装；`Communication/` 负责 UART/DMA 和 FDCAN 接收运行时封装。

这一层只负责“板上的硬件能被稳定使用”，不负责机器人逻辑。它位于队内代码的最底部，日常
不用修改。

### 6. 芯片底层：`Core/` 和 `Drivers/`

`Core/` 是 CubeMX 生成的外设初始化、IRQ 和 HAL 桥接；`Drivers/` 是 ST 官方 CMSIS、
STM32H7 HAL/LL 和芯片头文件。它们负责芯片启动和外设实现，不承载队内业务，日常不用修改。

### 7. 系统中间件：`Middlewares/`

`Middlewares/Third_Party/FreeRTOS/` 提供 FreeRTOS 内核、CMSIS-RTOS2 适配、可移植层和 heap。
它负责任务调度、队列、信号量和内存管理，不负责电机或机构控制，日常不用修改。

### 8. 测试和调试配套：`User/Tests/`、`上位机/`

`User/Tests/` 是不加入固件的主机侧回归测试；`上位机/` 是 Windows 下的命令、协议、参数和
联调工具。它们可以读取状态、发送测试命令、记录日志和辅助标定，但不能替代板端状态机，
也不属于日常板端功能修改入口。`User/Driver/Vofa/` 同样只用于台架电机调试，不加入生产目标。

### 9. 调用关系

统一遵循：

```text
应用层
  ↓
业务层
  ↓
算法层 / 驱动层
  ↓
硬件抽象层
  ↓
芯片底层 / 系统中间件
```

说明：

- 上位机/脚本层可以和应用层通信，但不能反过来把板端核心逻辑依赖在脚本上。
- 比赛时机器人必须在脱离上位机后也能独立运行。
- 脚本适合做测试、标定、记录和辅助分析，不适合承载核心闭环控制。

### 10. 分层原则

代码分层的目的不是为了好看，而是为了：

1. 日常需求集中在 `User/Application/`，降低误改底层的风险。
2. 改驱动时不影响上层逻辑。
3. 改算法时不需要改底层通信。
4. 比赛现场出问题时能快速判断是哪一层出了问题。

---

## 十六、团队原则（最重要）

> 代码是给队友看的，其次才是给电脑运行的。

优秀的比赛代码应该做到：

1. 统一命名，不用猜变量含义。
2. 统一格式，任何成员打开都能快速定位。
3. 模块解耦，修改一个功能不影响其他功能。
4. 参数集中管理，避免到处修改数字。
5. 可调试、可维护、可复用，比赛现场出现问题能迅速定位和修复。
